# 北極星 MVP後P2完了整理とP3着手判断

更新日: 2026-05-08

## 1. 目的

- MVP後P2の完了状況を記録する
- P3へ進む前の基準点を作る
- 次に着手する候補を比較する

## 2. MVP後P1 / P2 完了サマリ

| フェーズ | PR | 内容 | 判定 |
|---|---|---|---|
| P1 | #195 | Home card 表示件数・並び順・snapshot依存仕様化 | 完了 |
| P1 | #196 | Rule Lab日本語入力 UI経路再確認 | 完了 |
| P1 | #197 | docs文字化け棚卸し範囲整理 | 完了 |
| P2 | #198 | Performance Summary 日本語ヘッダー対応 | 完了 |
| P2 | #199 | disclosure / earnings 保存導線固定 | 完了 |
| P2 | #200 | snapshot stale / market_status unknown 整理 | 完了 |
| P2 | #201 | StrategyLab主要文言テスト固定 | 完了 |

補足:

- P1は「運用期待値の整理と正本docs整備」が中心だった。
- P2は「既存主要導線の実用性・回帰防止・運用品質向上」が中心だった。
- P2完了時点では、新規大型機能よりも live データ確認と旧正本文書の可読性確認を優先する。

## 3. P2で完了した改善

### 3.1 Performance Summary 日本語ヘッダー対応

- TradingView CSV import で `Performance Summary` 日本語ヘッダーを受け付けられるようにした。
- 既存の以下は維持した。
  - Performance Summary 英語ヘッダー
  - List of Trades 英語ヘッダー
  - List of Trades 日本語ヘッダー
  - UTF-8 BOM
  - invalid CSV の failed import 保存
- 未解決なのは、実TradingView日本語 UI で出る別ヘッダーの追加確認である。

### 3.2 disclosure / earnings 保存導線固定

- TDnet collector の `disclosure / earnings` について、fetch mock fixture で symbol match、earnings 判定、保存導線を固定した。
- `2148` 相当 title で `disclosure / earnings` の両方が保存されることを確認した。
- `5132` 相当 title で `disclosure` のみ保存され、earnings は `tdnet_symbol_match_but_no_earnings_title` として説明できることを確認した。
- `source_breakdown` / `diagnostics` / `0件 reason` の観測経路も維持した。

### 3.3 snapshot stale / market_status unknown 整理

- `daily_source_closed`
- `candidate_unknown`
- `freshness_invalid`
- `freshness_expired`
- `open_but_stale`
- `jp_market_holiday`
- `jp_market_weekend`
- `outside_jp_session`
- `closed_by_source`

上記 reason code の意味と、Home / SymbolDetail / Comparison での扱いを整理した。

整理結果:

- stale / unknown は原則 failure ではなく partial success と扱う
- snapshot がない場合でも row や画面全体は落とさない
- UI上の見え方改善は後続タスクに分ける

### 3.4 StrategyLab主要文言テスト固定

- StrategyLab の初期表示文言を frontend test で固定した。
- 生成結果、CSV取込、CSV失敗ガイダンスの主要文言も固定した。
- Rule Lab / Pine / CSV import の runbook と実UI文言のズレを抑える状態にした。

## 4. P2で残った課題

- 実TradingView日本語 Performance Summary CSV の別ヘッダー確認
- 実開示あり銘柄の live データ確認は `2148` / `5132` / `9929` まで完了
- `2148` では TradingView payload互換の疑似送信により、`disclosure / earnings` direct type が alert summary context / 本文へ入ることを確認済み
- `9929` では `disclosure / earnings` の保存導線と SymbolDetail API 相当の `related_references` 内訳まで確認済み
- direct type の TradingView 実送信再確認は、現時点では実施しない。必要なら実開示日近傍の任意後続確認とする
- stale / unknown の UI補助表示
- StrategyLab の動的エラー文言テスト
- docs/30 後半の破損節をどう扱うかの継続判断

補足:

- いずれも P2 作業で前提は整ったが、まだ live 実運用確認または追加固定が必要な項目である。
- これらは「MVP blocker ではないが、次の品質段階で早めに片付けるべき残課題」として扱う。

## 5. P3候補

| 候補 | 目的 | 効果 | コスト感 | 今すぐ着手するか |
|---|---|---|---|---|
| browser-based E2E導入 | 実画面回帰の自動化 | 高い | 高い | まだ早い |
| 複数ポートフォリオUI | 運用対象拡張 | 中〜高 | 高い | 後回し |
| transaction履歴編集UI | positions運用改善 | 中 | 中 | 後回し |
| 非日本銘柄の本格対応 | 適用市場拡張 | 中〜高 | 高い | 後回し |
| referencesあり銘柄での追加 TradingView 実送信 | 既存 alert 導線の品質補強 | 中 | 低〜中 | 2026-05-08 に `2148` の実送信1件を確認。direct type は疑似送信で確認済み |
| docs/29 / docs/30 個別可読性確認 | 正本 docs の読解性確保 | 中 | 低〜中 | 2026-05-08 に個別確認済み。`docs/29` は可読、`docs/30` は前半可読・後半破損のため参照注意付きで維持 |
| disclosure / earnings live データ追加確認 | references 供給の live 品質確認 | 高い | 低〜中 | 2026-05-08 に一次完了 |

判断:

- いきなり大きなUI拡張に入るより、MVP実運用品質の確認を続ける方が安全である。
- 特に `disclosure / earnings` と TradingView 実送信は、既存MVP導線の品質補強として直接価値がある。
- browser-based E2E は有用だが、導入コストがやや高く、P2で残った live 確認の後でよい。

## 6. 次に進める推奨候補

推奨順:

1. `SymbolDetail` の最小移行、または `SideRail` の `/api/home` 再取得最適化判断
2. browser-based E2E の対象拡張判断
3. transaction履歴編集UI
4. 複数ポートフォリオUI
5. 非日本銘柄の本格対応

理由:

- disclosure / earnings live データ追加確認 は 2026-05-08 時点で一次確認を完了した。
- references あり銘柄での TradingView 実送信自体は 2026-05-08 に `2148` で 1 件確認した。
- その後、TradingView payload互換の疑似送信で `disclosure / earnings` direct type が alert summary context / 本文へ入ることを確認した。
- `9929` の保存導線確認は完了した。
- direct type 実送信再確認は、alert 用 lookback が `disclosure=3日`, `earnings=7日` であることと、TradingView 実送信では過去 `triggered_at` を固定できないことから、現時点では実施しない判断に整理した。
- browser-based E2E の local-only PoC 自体は導入済みだが、Tailwind 化前に spec を増やしすぎると UI 改修コストに対して test 修正コストが先に膨らむ。
- `docs/46` に従う Tailwind 設定追加と最小 global CSS 整理は完了した。
- `docs/47` に基づく画面導線 / IA / ナビゲーション方針整理も完了した。
- `AppLayout` / `PageHeader` / `Navigation` / `TextLink` の最小土台を `Home` / `SymbolDetail` にだけ適用済みである。
- `SideRail` の最小 PoC は `Home` / `SymbolDetail` にだけ導入済みであり、`/api/home` 再利用、タブ切り替え、折りたたみ、既存管理画面への補助導線までを確認した。
- `SideRail` の watchlist / positions CRUD は 2026-05-09 時点で最小モーダル化まで完了し、既存 `/watchlist` `/positions` は移行期の補助画面として残している。
- `SideRail` の CRUD 成功後は `/api/home` `/api/watchlist-items` `/api/positions` を単位に再取得する。二重 fetch や再取得粒度の最適化は後続判断とする。
- `Home` の最小移行は 2026-05-09 時点で完了し、watchlist / positions の詳細一覧は `SideRail` に寄せた。
- 次の判断点は、`SymbolDetail` の最小移行を先に進めるか、`SideRail` の再取得最適化を先に行うかである。
- `SectionCard` など画面内部 component は、導線変更後の構造に合わせて抽出する。
- docs/29 / docs/30 の個別可読性確認は完了し、`docs/29` は可読、`docs/30` は全文復元せず後続 docs と実装を優先参照する方針を固定した。
- `docs/45` で browser-based E2E の方針整理を完了し、そのまま Playwright local-only の初回 PoC を `Home → SymbolDetail` で導入した。
- 次に見るべき論点は CI ではなく、画面導線 / IA / ナビゲーション方針を固め、その後に PoC 対象を Rule Lab / Backtest へ広げる価値があるかを判断することである。
- browser-based E2E 以降は価値は高いが、導入規模や仕様整理コストが上がる。

## 7. 次タスク用Codex指示テンプレート

### 7.1 disclosure / earnings live データ追加確認

```text
北極星プロジェクトのMVP後フェーズとして、disclosure / earnings の live データ追加確認を進めてください。

最重要前提:
- docs 配下を正本として扱ってください。
- 既存の collector / diagnostics / save path は壊さないでください。
- 今回は実装全面改修ではなく、live データで成立範囲を確認することが目的です。
- 外部アクセスは対象日・対象銘柄・回数を限定してください。
- raw HTML や secret 実値は docs / PR / log に残さないでください。

今回の目的:
1. 実開示あり銘柄で disclosure / earnings が live でも保存・表示されるか確認する
2. SymbolDetail / Alert summary / references 内訳への反映を確認する
3. 0件 reason と diagnostics の妥当性を再確認する
4. docs/33 と docs/39 に結果を反映する
```

### 7.2 referencesあり銘柄での追加 TradingView 実送信

```text
北極星プロジェクトのMVP後フェーズとして、references あり銘柄での TradingView 実送信追加確認を進めてください。

最重要前提:
- docs 配下を正本として扱ってください。
- 実送信は必要最小限の 1 件に留めてください。
- secret / token / shared_secret 実値は docs / PR / log に残さないでください。

今回の目的:
1. references が 0 件ではない銘柄で alert を 1 件送る
2. webhook / references / alert summary の導線が成立するか確認する
3. 背景材料ありの alert summary 品質を確認する
4. docs/35 または docs/32 に結果を追記する
```

### 7.3 docs/29 / docs/30 個別可読性確認

```text
北極星プロジェクトのMVP後フェーズとして、docs/29 / docs/30 の個別可読性確認を進めてください。

最重要前提:
- docs 配下を正本として扱ってください。
- 今回は全 docs 修復ではなく、対象 docs の可読性確認と修復方針整理が目的です。
- PowerShell 表示上の文字化けとファイル実体の文字化けを分けて扱ってください。
- 推測による本文復元は避けてください。

今回の目的:
1. UTF-8 明示読み取りと GitHub 表示基準で docs/29 / docs/30 の可読性を再判定する
2. 現行実装判断に必要な章を切り分ける
3. 修復するならどこまでか、修復しないなら何を正本にするかを決める
4. docs/42 と docs/39 に結果を反映する
```

## 8. 今回やらないこと

- P3実装への着手
- UI大規模改修
- API shape変更
- DB構造変更
- browser-based E2E の導入
- 複数ポートフォリオUI実装
- transaction履歴編集UI実装
- 非日本銘柄対応実装

## 9. 現時点の結論

- MVP後P1は完了済みである。
- MVP後P2も一区切りとして完了扱いでよい。
- 次は大きな拡張へ進む前に、live データ確認と正本文書の可読性確認を先に進めるのが安全である。
- 北極星の次フェーズは、P3大型実装そのものではなく、既存MVP導線の live 品質確認を挟んでから始める。

## 追記（2026-05-09）

- `SymbolDetail` の最小移行を完了した。
- snapshot / 最新アラート / 最新AI論点カード / Research Note / 関連参照情報の section 表示を整理した。
- 次の判断点は、銘柄起点ストラテジー適用フロー設計、または `SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-09 その2）

- 銘柄起点ストラテジー適用フロー設計を完了し、正本を [docs/48.北極星 銘柄起点ストラテジー適用フロー設計（P3）.md](./48.北極星%20銘柄起点ストラテジー適用フロー設計（P3）.md) に固定した。
- `StrategyLab` は作成 / 生成側、`SymbolDetail` は適用 / 検証入口、`BacktestDetail` は検証レポート詳細として役割分離を維持する。
- 次の候補は `SymbolDetail` への `ストラテジー / 検証結果` 空 section 追加、`StrategyList` / `StrategyDetail` 設計、DB 保存概念整理、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-09 その3）

- `SymbolDetail` に `ストラテジー / 検証結果` の空 section を追加した。
- 現時点では準備中表示と既存 `/strategy-lab` `/backtests` への補助導線のみを持つ。
- 次の候補は `StrategyList` / `StrategyDetail` の画面設計、DB 保存概念整理、`SymbolDetail` から既存 strategy version を選ぶ最小 UI、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-09 その4）

- `StrategyList` / `StrategyDetail` の画面設計を完了し、正本を [docs/49.北極星 StrategyList・StrategyDetail 画面設計（P3）.md](./49.北極星%20StrategyList・StrategyDetail%20画面設計（P3）.md) に固定した。
- `StrategyList` / `StrategyDetail` は再利用可能な Strategy Definition の一覧 / 詳細であり、`BacktestList` / `BacktestDetail` を置き換えない。
- 次候補は `StrategyList` placeholder route、`StrategyDetail` placeholder route、DB 保存概念整理、`SymbolDetail` から既存 strategy version を選ぶ最小 UI、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-09 その5）

- `/strategies` に `StrategyList` placeholder route を追加した。
- ここでは再利用可能な Strategy Definition を扱い、`StrategyLab` は作成 / 生成入口、`BacktestList` / `BacktestDetail` は検証レポート導線として役割分離を維持する。
- 次候補は `StrategyDetail` placeholder route、DB 保存概念整理、existing strategy data display、`SymbolDetail` apply flow、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-10）

- `/strategies/:strategyId` に `StrategyDetail` placeholder route を追加した。
- 現時点では version 一覧・`StrategyLab`・`BacktestList` への補助導線のみを持ち、strategy data の実表示や apply flow は未接続である。
- 次候補は DB 保存概念整理、existing strategy data display、`SymbolDetail` apply flow、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-10 その2）

- Strategy / symbol application の DB 保存概念整理を完了し、正本を [docs/50.北極星 Strategy保存概念整理（P3）.md](./50.北極星%20Strategy保存概念整理（P3）.md) に固定した。
- 次候補は existing strategy data display、strategy metadata migration decision、`SymbolDetail` apply UI、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-10 その3）

- `StrategyList` / `StrategyDetail` の existing strategy data display を追加した。
- 次候補は strategy metadata migration decision、`SymbolDetail` apply UI、related reports / applied symbols display、`SideRail` の `/api/home` 再取得最適化である。

## 追記（2026-05-10 その4）

- Strategy metadata migration decision を完了し、正本を [docs/51.北極星 Strategy metadata migration decision（P3）.md](./51.北極星%20Strategy%20metadata%20migration%20decision（P3）.md) に固定した。
- 初回は `StrategyRule.status` を `active` / `archived` に使い、hard delete と favorite / usage metadata は後続判断とする。
- 次候補は `StrategyList` status filter、archive / restore API、`SymbolDetail` apply UI、related reports / applied symbols display、`SideRail` refresh optimization である。

## 追記（2026-05-10 その5）

- `StrategyList` status filter と archive / restore API を追加した。
- `StrategyRule.status` を `active` / `archived` として使う方針を実装に反映した。
- 次候補は `SymbolDetail` apply UI、related reports / applied symbols display、favorite / usage metadata 再判断、`SideRail` refresh optimization である。

## 追記（2026-05-10 その6）

- `SymbolDetail` apply UI の第一段階として、既存 active strategy / version を選ぶ selection-only UI を追加した。
- 選択内容は未保存であり、Symbol Strategy Application 保存、CSV 取込、内部バックテスト接続は未実装である。
- 次候補は Symbol Strategy Application DB/API 設計、`SymbolDetail` apply 保存処理、related reports / applied symbols display、favorite / usage metadata 再判断、`SideRail` refresh optimization である。

## 追記（2026-05-10 その7）

- Symbol Strategy Application の DB / API 設計を完了し、正本を [docs/52.北極星 Symbol Strategy Application DB・API設計（P3）.md](./52.北極星%20Symbol%20Strategy%20Application%20DB・API設計（P3）.md) とした。
- 推奨方針は、application parent table と application run table を分け、`BacktestDetail` は個別検証レポート詳細として維持する案である。
- 次候補は Prisma schema draft / migration、GET symbol applications API、POST symbol application API、`SymbolDetail` apply 保存処理、CSV import / internal backtest wiring、`SideRail` refresh optimization とする。

## 追記（2026-05-10 その8）

- Symbol Strategy Application の Prisma schema draft / migration を完了した。
- application parent と application run table を追加し、run から `Backtest` / `BacktestImport` / `InternalBacktestExecution` へ link できる土台を作った。
- API / frontend / 保存処理は未実装であり、次候補は GET symbol applications API、POST symbol application API、`SymbolDetail` apply 保存処理、CSV import wiring、internal backtest wiring、`SideRail` refresh optimization とする。

## 追記（2026-05-10 その9）

- GET symbol applications API を完了した。
- read-only API のみであり、POST 保存処理、frontend UI 接続、CSV import wiring、internal backtest wiring は未実装である。
- 次候補は POST symbol application API、`SymbolDetail` apply 保存処理、CSV import wiring、internal backtest wiring、related reports / applied symbols display、`SideRail` refresh optimization とする。

## 追記（2026-05-10 その8）

- Symbol Strategy Application の POST 保存 API を追加した。
- symbol / strategy / strategy version の存在確認、archived strategy の拒否、strategy-version 所属確認、active duplicate 防止を backend 側で扱う。
- 次の判断点は `SymbolDetail` apply保存処理、CSV import wiring、internal backtest wiring、related reports / applied symbols display である。

## 追記（2026-05-10 その9）

- `SymbolDetail` で active strategy / version を選択し、Symbol Strategy Application として保存できるようにした。
- 保存済み application 一覧は `GET /api/symbols/:symbolId/strategy-applications` で表示する。
- CSV取込、内部バックテスト、application run 作成は未接続である。

## 追記（2026-05-10 その10）

- 保存済み Symbol Strategy Application から TradingView CSV を取り込めるようにした。
- CSV取込は Backtest / BacktestImport / SymbolStrategyApplicationRun を作成し、保存済み application の latest run / latest report に反映する。
- internal backtest wiring、related reports / applied symbols display、application archive / restore API、BacktestDetail backlink は後続で扱う。

## 追記（2026-05-10 その11）

- 保存済み Symbol Strategy Application から internal backtest を開始できるようにした。
- InternalBacktestExecution と SymbolStrategyApplicationRun を作成し、application の latest run に反映する。
- Backtest / BacktestImport 作成や BacktestDetail report 化は行わず、result detail 表示は後続で扱う。
- 次候補は related reports / applied symbols display、BacktestDetail backlink、application archive / restore API、internal execution result display from `SymbolDetail`、`SideRail` refresh optimization である。

## 追記（2026-05-10 その12）

- related reports / applied symbols display を完了扱いにした。
- `StrategyDetail` / `SymbolDetail` / `BacktestDetail` で application / run / report の関係を read-only に確認できるようになった。
- application archive / restore、internal execution result detail、BacktestDetail redesign は未実装である。
- 次候補は BacktestDetail backlink refinement、application archive / restore API、internal execution result display from `SymbolDetail`、`SideRail` refresh optimization、UI component cleanup とする。

## 追記（2026-05-10 その13）

- Symbol Strategy Application の archive / restore API と、`SymbolDetail` / `StrategyDetail` からの最小操作を追加した。
- hard delete は未実装であり、履歴保持のため run / report link は維持する。
- 次の判断点は internal execution result display、BacktestDetail backlink refinement、SideRail refresh optimization、UI component cleanup である。

## 追記（2026-05-10 その9）

- `SymbolDetail` から internal backtest execution の status / result summary を read-only で確認できるようにした。
- queued / running / failed / succeeded の状態を最小表示し、succeeded の場合は result summary の主要 metrics を表示する。
- `BacktestDetail` report 化、redesign、DB / API 変更は行っていない。

## 追記（2026-05-10 その14）

- BacktestDetail backlink refinement を完了した。
- 銘柄起点 application 由来の report では、BacktestDetail から SymbolDetail / StrategyDetail / StrategyVersionDetail へ戻る導線を整理した。
- application 由来でない Backtest は従来どおり report detail として扱う。
- 次の判断点は SideRail refresh optimization、UI component cleanup、Playwright scenario 追加である。

## 追記（2026-05-10 その15）

- SideRail refresh optimization を完了した。
- Home と SideRail の `/api/home?summary_type=latest` 利用を整理し、Home では既存 SWR data / mutate を SideRail に渡す方針にした。
- CRUD 後の refresh 範囲を watchlist 系と positions 系で分離した。
- 次の判断点は UI component cleanup、Playwright scenario 追加、SideRail 以外の表示整理である。

## 追記（2026-05-10 その16）

- UI component cleanup の第一段として、section / card 表現の共通化を最小範囲で開始した。
- `SectionCard` は Home / SymbolDetail の既存 section wrapper を置き換える目的に限定し、Strategy / Backtest / Application 仕様には触れていない。
- 次の判断点は適用範囲の拡大、Button / EmptyState 共通化、Playwright scenario 追加である。

## 追記（2026-05-10 その17）

- P3 の主要 read-only 導線を browser smoke として追加した。
- 既存 seed に最小の Symbol Strategy Application / run fixture を追加し、SymbolDetail から StrategyDetail と BacktestDetail へ安定して遷移できるようにした。
- 次候補は追加 Playwright scenario の拡張、SideRail 以外の UI cleanup、または P3 導線の残課題整理とする。

## 追記（2026-05-10 その18）

- `SymbolDetail` の `ストラテジー / 検証結果` section の肥大化に対し、表示整理の第一歩を実施した。
- 保存済み application / run / report の read-only 情報を見やすくするため、row 内の表示責務を小さく分割した。
- 次候補は UI component cleanup の追加範囲判断、または P3 導線の残課題整理とする。

## 追記（2026-05-10）

- `BacktestDetail` の表示整理を実施した。
- `銘柄起点の適用情報` section は既存の表示内容とリンクを維持したまま、BacktestDetail 内の private component に分割した。
- API / backend / DB 変更はなく、P3 の表示安定化タスクとして扱う。

## 追記（2026-05-10）

- P3 の進捗整理として [[53.北極星 P3現在地と残課題整理（P3）]] を追加した。
- P3 の中心導線は Home / SideRail / SymbolDetail / StrategyDetail / BacktestDetail まで read-only smoke で固定済みとし、次は residual task の優先順位判断へ進む。
- hard delete、favorite metadata、SymbolBacktestDetail / StrategyBacktestDetail、大規模 redesign は後続フェーズ送りとする。

## 追記（2026-05-10）

- internal backtest result の Backtest report 化について、P3内の最小方針を追加した。
- succeeded execution のみ `Backtest` 化し、`SymbolStrategyApplicationRun` から execution と report の両方を参照する。
- failed / canceled / running execution、AI summary generation、BacktestDetail redesign は引き続き後続判断とする。

## 追記（2026-05-10）

- `SymbolDetail` から internal backtest result を Backtest report 化する最小 UI 接続を追加した。
- API / backend / DB は変更せず、PR #250 の conversion API を利用する。
- failed / canceled / running / queued execution の report 化、AI summary 自動生成、BacktestDetail redesign は後続判断とする。

## 追記（2026-05-10）

- P3 の internal backtest report 化に続き、BacktestDetail で importless report を自然に確認できる表示を追加した。
- BacktestImport は CSV import 専用のまま維持し、internal backtest 由来 report では result summary / artifact pointer を snapshot から read-only 表示する。
- BacktestDetail の全面 redesign や AI summary 生成仕様変更は後続判断とする。

## 追記（2026-05-10）

- Backtest AI summary の internal backtest report 文脈整理を追加した。
- internal backtest 由来 report では `BacktestImport` 不在をエラー扱いせず、result summary / artifact pointer / execution id を AI summary input として扱う。
- AI summary endpoint と UI 導線は既存のまま維持し、自動生成や BacktestDetail redesign は行わない。

## 追記（2026-05-10）

- BacktestDetail の artifact 表示を小さく整理した。
- internal backtest 由来 report の `artifact_pointer` は概要 field と raw JSON の両方で確認できる。
- artifact file の実体読込、download、artifact schema 変更、BacktestDetail redesign は行わない。

## 追記（2026-05-10）

- internal backtest report 化まわりは、P3内で「保存済み application 起点の report 化から BacktestDetail 確認まで」一通り接続済みと整理する。
- `BacktestImport` を作らない importless report 方針、`SymbolStrategyApplicationRun` に execution と report を併存させる方針、guarded update による冪等化方針を維持する。
- AI summary 自動生成、artifact download、internal polling 本格化、Visual regression は後続判断とする。

## 追記（2026-05-10）

- internal backtest report 系の追加 smoke は read-only 1本に限定する。
- seed 済み importless Backtest report を使い、BacktestDetail の internal report section / BacktestImport なし説明 / execution id / result summary / artifact pointer / backlink を確認する。
- 実行系操作、conversion API 呼び出し、AI summary 生成、Visual regression は Playwright の対象外とする。

## 追記（2026-05-10）

- P3完了判断に向け、PR #237〜#256 の最終棚卸しを docs/53 に集約した。
- P3 は銘柄起点 Strategy Application / report 導線、internal backtest report 化、BacktestDetail 表示、read-only smoke、UI構造土台までを完了扱いにできる。
- Tailwind / UI構造安定化は土台実装と一部画面適用まで完了とし、UX改善や未対応画面整理は後続タスクとして扱う。

## 追記（2026-05-10）

- P3完了宣言と次フェーズ着手判断を docs/53 に追記した。
- P3完了を妨げない残課題は、UX改善、共通UI component、pagination / filter、比較 UX、自動生成 / artifact 運用である。
- 次フェーズにすぐ入る場合は、UI / UX cleanup、application / run 表示改善、CSV / internal report 比較 UX の順を推奨する。

## 追記（2026-05-10）

- 次フェーズ初手として UI / UX cleanup を開始し、共通状態表示 component を最小導入した。
- `EmptyState` / `ErrorState` は `StrategyDetail` の一部 empty / error 表示に限定適用した。
- 画面全体の redesign、LoadingState / Button / StatusBadge の共通化、API / backend / DB 変更は行わない。

## 追記（2026-05-10）

- `EmptyState` / `ErrorState` の適用範囲を `BacktestDetail` に限定拡張した。
- BacktestDetail の取得失敗、取込データなし、主要指標なし、AI総評未生成を共通状態表示に寄せた。
- API / backend / DB / Prisma schema、BacktestDetail の主要仕様、CSV / internal backtest report 表示は変更しない。

## 追記（2026-05-10）

- `EmptyState` / `ErrorState` の適用範囲を `SymbolDetail` に限定拡張した。
- SymbolDetail の取得失敗、最新アラートなし、保存済み application なし / 取得失敗、Research Note なしを共通状態表示に寄せた。
- API / backend / DB / Prisma schema、Strategy Application、CSV / internal backtest / report conversion の挙動は変更しない。

## 追記（2026-05-10）

- 次フェーズ UI / UX cleanup として `LoadingState` を最小導入した。
- 適用先は `StrategyDetail` / `BacktestDetail` の loading 表示に限定した。
- API / backend / DB / Prisma schema、Button / StatusBadge / KeyValueRow / DataList の共通化は変更しない。

## 追記（2026-05-10）

- `EmptyState` / `ErrorState` / `LoadingState` の第一段階導入を完了扱いとする。
- 適用済みは `StrategyDetail` / `BacktestDetail` / `SymbolDetail` の一部であり、未適用画面は後続判断とする。
- 次に UI cleanup を進める場合は、操作表現を揃えるため `Button` 共通化を優先する。

## 追記（2026-05-10）

- `Button` 共通化の初手として、`StrategyDetail` の archive / restore 操作ボタンへ限定適用した。
- 表示文言、disabled の意味、archive / restore の挙動は維持した。
- `SymbolDetail` / `BacktestDetail` の操作ボタン、`StatusBadge` / `KeyValueRow` / `DataList` は後続判断として残す。

## 追記（2026-05-10）

- `Button` 共通化の適用範囲を `SymbolDetail` に限定拡張した。
- 保存済み application の archive、適用保存、AI論点カード生成 / 再生成の操作ボタンを対象にした。
- CSV import / internal backtest / report conversion の実行ボタンは後続判断として残す。

## 追記（2026-05-11）

- `StatusBadge` 共通化の初手として、`StrategyDetail` / `SymbolDetail` の一部 status 表示へ限定適用した。
- active / generated / succeeded などの既存 status 文字列は変更せず、表示 component だけを整理した。
- `KeyValueRow` / `DataList` や status enum の本格整理は後続判断として残す。

## 追記（2026-05-11）

- `StatusBadge` 共通化の適用範囲を `BacktestDetail` に限定拡張した。
- report status、parse status、application / run status の表示を対象にし、表示文言と導線は維持した。
- status enum の本格整理、`KeyValueRow` / `DataList`、BacktestDetail redesign は後続判断として残す。

## 追記（2026-05-11）

- `KeyValueRow` / `KeyValueList` 共通化の初手として、`BacktestDetail` の一部 key-value 表示へ限定適用した。
- 基本情報と銘柄起点 backlink の読みやすさを小さく揃え、BacktestDetail redesign は行わない。
- JSON viewer 共通化、DataTable 追加、artifact raw 表示整理は後続判断として残す。

## 追記（2026-05-11）

- UI共通 component 第一巡を完了扱いにする。
- 適用済み画面は `StrategyDetail` / `SymbolDetail` / `BacktestDetail` の一部であり、未対応画面と JSON viewer / DataTable / form input / modal は後続判断として残す。
- 次に進む場合は、application / run 表示改善、CSV / internal report 比較 UX、未対応画面の Tailwind整理を優先候補とする。

## 追記（2026-05-11）

- `SymbolDetail` の保存済み application / run / report 表示を既存 UI component の範囲で整理した。
- run count、strategy version、latest run、latest report の見通しを改善し、CSV import / internal backtest / report conversion / archive の挙動は維持した。
- pagination / filter と比較 UX は後続判断として残す。

## 追記（2026-05-11）

- CSV import report と internal backtest report の比較 UX 方針を整理した。
- CSV は TradingView 実行結果、internal は Polaris engine 実行結果として役割を分け、まずは既存 API でできる read-only 比較導線を優先候補にする。

## 追記（2026-05-11）

- CSV / internal report 比較 UX の最小実装として、`StrategyDetail` / `SymbolDetail` の report 由来表示を整理した。
- 既存 API の `execution_source` を使う範囲に限定し、comparison route / DB 変更 / metrics 正規化は後続判断とする。

## 追記（2026-05-11）

- `BacktestDetail` に同一 application の関連 report 導線を追加した。
- CSV import report と internal backtest report は、まず相互に辿れる read-only link として比較 UX の初手を進め、新規 comparison route は後続判断とする。

## 追記（2026-05-11）

- CSV / internal report 比較 UX 第一段階を完了扱いにする。
- 新規比較画面や DB 変更なしで、既存画面上の report 由来表示と同一 application 関連 report 導線まで接続済みとした。
- metrics 比較 UI、AI summary 比較、artifact diff、Visual regression は後続判断として残す。

## 追記（2026-05-11）CSV / internal report metrics 比較補助

- 次フェーズ UI / UX cleanup の一環として、`BacktestDetail` に同一 application 配下 report の metrics 横並び比較補助を追加した。
- 既存 API / relation で読める metrics に限定し、comparison entity、metrics normalization table、新規比較画面は追加していない。
- 後続では、metrics 欠損の扱い、SymbolDetail での比較導線、または本格比較画面の必要性を判断する。

## 追記（2026-05-11）CSV / internal report metrics 比較補助完了

- CSV / internal report 比較 UX は、既存画面と既存 relation 由来の read-only 補助として一段完了扱いにする。
- `BacktestDetail` では同一 application の current / related report metrics を横並びで確認できる。
- 本格比較画面、metrics 正規化、AI summary 比較、artifact diff、Visual regression は後続判断として残す。

## 追記（2026-05-11）SymbolDetail latest pair 表示

- `SymbolDetail` の saved application row で、同一 application 配下の CSV import report と internal backtest report の latest pair を確認できるようにした。
- `latest_reports_by_source` は read-only optional response として追加し、既存 API の主要 shape、DB schema、CSV import / internal backtest / report conversion の挙動は変更しない。
- 比較実行や新規比較画面ではなく、BacktestDetail metrics 比較補助へ進む入口として扱う。

## 追記（2026-05-11）CSV / internal report 比較 UX 現時点完了

- CSV / internal report 比較 UX は、既存画面と read-only response 中心の範囲で現時点完了扱いにする。
- 完了範囲は `StrategyDetail` / `SymbolDetail` の report type / source 表示、`BacktestDetail` の同一 application 関連 report 導線と metrics 横並び比較補助、`SymbolDetail` の latest pair 表示である。
- 新規比較画面、comparison entity、metrics normalization table、AI summary 比較、artifact diff、Visual regression は後続判断として残す。

## 追記（2026-05-11）SymbolDetail application filter

- `SymbolDetail` の saved application row に、既存 response を使った最小 filter / grouping を追加した。
- report あり / report なしの client-side filter と CSV / internal report 件数 summary に限定し、server-side filter、pagination、本格 report list は後続判断とする。

## 追記（2026-05-11）SymbolDetail application / report 表示改善完了

- `SymbolDetail` の application / report 表示改善は、既存 API response を使う範囲で完了扱いにする。
- saved application / latest run / latest report の情報階層、CSV / internal latest pair、client-side filter、表示件数 summary まで接続済みである。
- CSV import、internal backtest、report conversion、archive、保存処理の挙動は維持し、本格 pagination / filter と report list grouping は後続判断とする。

## 追記（2026-05-11）Symbol Strategy Application pagination / filter 方針

- Symbol Strategy Application / run の server-side pagination / filter は、P3完了後の次フェーズ設計として docs/53 に整理済み。
- 現時点の client-side filter は完了扱いとし、server-side へ進む場合は既存 endpoint の optional query 拡張を第一候補にする。
- DB migration / Prisma schema change は初回方針では不要とし、大量データ時の index / read model は後続判断とする。

## 追記（2026-05-11）Symbol Strategy Application server-side filter 最小実装

- 既存 `GET /api/symbols/:symbolId/strategy-applications` の後方互換拡張として、`status=all` と `report_presence` filter を最小実装した。
- `SymbolDetail` の report 有無 filter は server-side query に接続済みで、未指定時の active application list は維持する。
- report source、strategy / version、run type / run status、archived application 本格一覧は後続判断として残す。

## 追記（2026-05-11）Symbol Strategy Application server-side filter 完了整理

- Symbol Strategy Application / run の server-side filter 第一段階は完了扱いにする。
- 完了範囲は既存 list endpoint の `status=active|archived|all` と `report_presence=with_reports|without_reports`、および `SymbolDetail` filter 接続である。
- 後続候補は `report_source`、run type / run status、archived application 本格一覧、application-specific runs / reports endpoint とする。

## 追記（2026-05-11）Symbol Strategy Application report_source filter

- Symbol Strategy Application server-side filter は、`status` / `report_presence` に続き `report_source=csv_import|internal_backtest` まで最小実装済みとする。
- `SymbolDetail` では report 有無 filter と source filter を組み合わせて application list を再取得できる。
- 後続候補は run type / run status filter、archived application 本格一覧 UI、application-specific runs / reports endpoint とする。

## 追記（2026-05-11）Symbol Strategy Application run_type / run_status filter 方針

- `run_type` / `run_status` filter は、初回は latest run 基準で既存 list endpoint に optional query として追加する方針にする。
- any run 履歴検索は application-specific runs endpoint の設計へ送り、SymbolDetail の saved application list では最新 run の状態確認に絞る。
- DB migration / Prisma schema change は初回方針では不要とする。

## 追記（2026-05-11）Symbol Strategy Application run_type / run_status filter 最小実装

- Symbol Strategy Application server-side filter は、`status` / `report_presence` / `report_source` に続き、latest run 基準の `run_type` / `run_status` まで最小実装済みとする。
- `SymbolDetail` では report 有無、report source、latest run type、latest run status を組み合わせて application list を再取得できる。
- any run 履歴検索は application-specific runs endpoint の後続設計へ送る。

## 追記（2026-05-11）Symbol Strategy Application server-side filter 第一段階完了

- Symbol Strategy Application server-side filter 第一段階は完了扱いにする。
- 既存 endpoint の後方互換を維持し、`status`、`report_presence`、`report_source`、latest-run 基準の `run_type` / `run_status` を `SymbolDetail` から利用できる。
- any run 履歴検索、application-specific runs / reports endpoint、archived application 本格一覧 UI は後続判断とする。

## 追記（2026-05-12）Application Detail / History usability pass 完了

- P3 後続の Application Detail / History usability pass は、read-only foundation の使いやすさ改善として完了扱いにする。
- 完了範囲は runs filter / pagination、reports filter / pagination、metrics 欠損値説明、SymbolDetail から Application Detail への browser smoke である。
- DB migration / Prisma schema change / comparison entity / metrics normalization table は導入していない。

## 追記（2026-05-12）Application Detail / History 完了整理

- P3 後続の Application Detail / History foundation + usability pass は完了扱いにする。
- SymbolDetail は overview と入口、Application Detail は run / report history の read-only 確認に責務を分ける。
- comparison entity、metrics normalization、artifact diff、AI summary 比較は後続判断として残す。

## 追記（2026-05-12）StrategyVersionDetail cleanup 段階1

- P3 後続の UI cleanup second pass として、`StrategyVersionDetail` の表示整理を段階1完了扱いにする。
- 既存 UI component の限定適用に留め、Pine generation / validation / backtest の仕様変更は行っていない。
- Visual regression、本格 form / modal / table 共通化、未対応画面の整理は後続判断とする。

## 追記（2026-05-12）StrategyLab + BacktestComparisonDetail cleanup 段階2

- P3 後続の UI cleanup second pass として、`StrategyLab` と `BacktestComparisonDetail` の表示整理を段階2完了扱いにする。
- API shape、backend、DB、Prisma schema、StrategyLab の生成・保存・検証、Backtest comparison 仕様は変更していない。
- Home / SideRail、form / modal / table component 本格共通化、Visual regression は後続判断とする。

## 追記（2026-05-12）Home / SideRail light cleanup 段階3

- P3 後続の UI cleanup second pass として、`Home` と `SideRail` の軽い表示整理を段階3完了扱いにする。
- API shape、backend、DB、Prisma schema、Home / SideRail の refresh 仕様、watchlist / positions CRUD 仕様は変更していない。
- form / modal / table component 本格共通化、Visual regression は後続判断とする。

## 追記（2026-05-12）UI cleanup second pass 完了整理

- P3 後続の UI cleanup second pass は、StrategyVersionDetail、StrategyLab、BacktestComparisonDetail、Home、SideRail の軽い整理までを完了扱いにする。
- 既存導線、API shape、backend、DB、Prisma schema、refresh / CRUD 挙動は変更していない。
- form / modal / table component 本格共通化、Visual regression、BacktestDetail / report comparison UX、application-specific runs / reports further UX は後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR1

- P3 後続の UI foundation third pass PR1として、form input / textarea / select の最小 component を追加した。
- 適用は StrategyLab と StrategyVersionDetail の一部に限定し、既存導線、生成・保存・検証、Pine workflow、API shape、backend、DB、Prisma schema は変更していない。
- form framework 導入、全画面一括適用、modal / table / JSON viewer 共通化、Visual regression は後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR2

- P3 後続の UI foundation third pass PR2として、既存 `Modal` を利用する最小 modal shell を追加した。
- 適用は SideRail の watchlist / positions CRUD modal に限定し、既存 CRUD 挙動、refresh 範囲、API shape、backend、DB、Prisma schema は変更していない。
- keyboard trap の大規模実装、table / data list、JSON viewer、Visual regression は後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR3

- P3 後続の UI foundation third pass PR3として、`JsonBlock` を追加し BacktestDetail の raw artifact JSON に限定適用した。
- ApplicationDetail / BacktestDetail / BacktestComparisonDetail の既存導線、AI summary、artifact 表示の意味、comparison、filter / pagination、API shape、backend、DB、Prisma schema は変更していない。
- RecordList / SimpleTable / DataTable、JSON diff、artifact diff、Visual regression は後続判断とする。

## 追記（2026-05-12）UI foundation third pass 完了整理

- P3 後続の UI foundation third pass は、FormFields / ModalShell / JsonBlock の薄い共通化までを完了扱いにする。
- 適用先は限定し、既存導線、API shape、backend、DB、Prisma schema は変更していない。
- DataList / SimpleTable / DataTable、JSON diff、artifact diff、Visual regression は後続判断とする。

## 追記（2026-05-13）Report comparison UX phase 2 PR1

- P3完了後の小さな表示改善として、BacktestDetail の同一 application 内 current / related report metrics 補助に source 差と metrics root の説明を追加する。
- ApplicationDetail は application 履歴探索、BacktestComparisonDetail は保存済み比較 / 本格比較候補として責務を維持する。
- comparison entity、metrics normalization、artifact diff、AI summary 自動比較は後続判断とする。

## 追記（2026-05-13）Report comparison UX phase 2 PR2

- ApplicationDetail の report履歴から BacktestDetail の比較補助へ進む入口を短い説明と link label で明確にする。
- BacktestComparisonDetail は保存済み pairwise comparison の再訪画面であり、本格比較画面候補である説明に留める。
- API / backend / DB / Prisma schema、新規 comparison entity、実行系操作は変更しない。

## 追記（2026-05-13）Report comparison UX phase 2 完了整理

- P3完了後の小さな docs / UX 整理として、Report comparison UX phase 2 PR1 / PR2 は完了扱いにする。
- `BacktestDetail` は個別 report detail と同一 application 内 metrics comparison helper、`ApplicationDetail` は run / report history、`BacktestComparisonDetail` は保存済み pairwise comparison の再訪画面として責務を分ける。
- CSV import report metrics は `BacktestImport` parsed summary、internal backtest report metrics は `strategySnapshotJson.result_summary` 由来として扱い、`-` は source 側に該当 metric がないことを示す。
- comparison entity、metrics normalization table、artifact diff / JSON diff、AI summary 自動比較、Visual regression は後続判断として残す。

## 追記（2026-05-13）Visual regression readiness docs-only 整理

- P3完了後の後続候補として Visual regression / screenshot comparison は残すが、今回は docs-only readiness 整理に留める。
- 初回 pilot は 1〜3 画面に絞り、`ApplicationDetail` / `SymbolDetail` を優先候補にする。
- `Home` は dynamic data、`BacktestDetail` は raw JSON / artifact / long page、AI / external data 表示は mask または対象外を検討する。
- CI required check 化は行わず、optional script / snapshot は pilot 対象確定後に検討する。

## 追記（2026-05-13）AI summary / artifact operations design

- P3後続の PR1 として、CSV import report と internal backtest report の AI summary input / artifact の扱いを docs/53 に整理した。
- `BacktestDetail` は個別 report の AI summary / artifact 表示、`ApplicationDetail` は report history 探索、`BacktestComparisonDetail` は保存済み pairwise comparison の再訪に責務を分ける。
- artifact file read / download / diff、AI summary 自動生成、AI summary 同士の比較は後続判断であり、P3完了判断を妨げない。

## 追記（2026-05-13）AI summary / artifact operations phase completion

- P3後続の AI summary / artifact operations phase は、PR #309〜#310 の範囲で完了扱いにする。
- CSV import report は `BacktestImport` / parsed summary / comparison diff / TradingView 文脈、internal backtest report は `strategySnapshotJson.result_summary` / `artifact_pointer` / `internal_backtest_execution_id` / importless 文脈として扱う。
- `BacktestDetail` は source-aware AI summary input と artifact metadata 確認、`ApplicationDetail` は report history 入口、`BacktestComparisonDetail` は保存済み pairwise comparison の AI summary 表示として責務を分ける。
- AI summary 自動生成 policy、AI summary 同士の比較 UX、artifact file read / download 権限境界、artifact diff / JSON diff、artifact metadata schema / retention policy、BacktestComparisonDetail 本格化、Visual regression pilot は後続判断とする。
- API / backend / DB / Prisma schema / test は変更しない。
