# 北極星 MVP受入確認結果（MVP）

## 1. 実施日
- 2026-05-07

## 2. branch / commit
- branch: `codex/mvp-acceptance-execution`
- base main: `06d3e38`（PR #191 取り込み済み）
- runtime verification baseline: `c915c5b`（PR #191 は docs-only のため runtime 差分なし）
## 3. 確認環境
- OS: Windows / PowerShell
- frontend: `http://localhost:5173`
- backend: `http://localhost:3000`
- in-app browser / UI確認: live API確認、既存runbook、直近確認記録、関連frontend testを併用
- TradingView実送信: 2026-05-06 実送信確認済み記録を参照（`docs/32`, `docs/35`）

## 4. DB状態
- `seed + 実データ混在`
- 確認時点で Home / Symbol / Alert / Backtest に seed と実運用レコードが共存

## 5. local LLM
- `ollama` プロセス起動を確認
- 実データ Comparison summary / Alert summary の既存成功記録あり
- 今回の live Comparison generate でも AI summary 生成成功を確認

## 6. 実行した build / test
- `pnpm --filter backend build`
- `pnpm --filter frontend build`
- `pnpm --filter backend test:e2e:home-symbol-comparison`
- `pnpm --filter backend test:e2e:backtest-import`
- `pnpm --filter backend test -- test/alert-summary-failed.e2e.test.ts`
- `pnpm --filter backend test -- test/queue-handlers.e2e.test.ts`
- `pnpm --filter frontend test -- src/pages/Home.test.tsx`
- `pnpm --filter frontend test -- src/pages/SymbolDetail.test.tsx`
- `pnpm --filter frontend test -- src/pages/ComparisonDetail.test.tsx`
- `pnpm --filter frontend test -- src/pages/BacktestDetail.test.tsx`

## 7. 必須チェック領域ごとの判定表

| 領域 | 判定 | 根拠 | メモ |
|---|---|---|---|
| A. 環境・起動 | OK | backend / frontend build 通過。`/api/home` と frontend root が HTTP 200。`node` / `ollama` / Docker 系プロセス起動。 | README / walkthrough に沿った起動導線に大きな破綻なし。 |
| B. Home | OK | `GET /api/home` で `watchlist_symbols` / `positions` / `recent_alerts` / `daily_summary` / `key_events` を確認。 | `daily_summary` は `insufficient_context=true` でも保守的に表示。 |
| C. watchlist / positions | OK | `/api/watchlist-items` と `/api/positions` で create / update / delete を live 実行。 | temp symbol の Home カード即時視認は表示制限・snapshot 依存で未再現。API CRUD は成立。 |
| D. SymbolDetail | OK | 7203 / 6758 の `GET /api/symbols/:symbolId` を確認。snapshot / chart symbol / references 内訳 / AI論点カード / recent_alerts / comparison導線データあり。 | Note は 7203 にあり、6758 は null。導線自体は成立。 |
| E. Comparison | OK | 7203 vs 6758 の live comparison session 作成と `POST /generate` 成功。`GET /api/comparisons/:id` で AI summary 本文あり。 | 片側 references 差がある比較も直近確認済み。 |
| F. TradingView webhook | OK（直近確認済み） | `docs/32` / `docs/35` に 2026-05-06 実送信確認あり。実送信で `webhook_receipts` / `alert_events` / `ai_jobs` / Home / SymbolDetail 反映まで成立。 | 今回は追加実送信なし。疑似 webhook 追加も不要と判断。 |
| G. Alert summary | OK（直近確認済み） | `docs/35` の実データ品質確認と live Home / Symbol recent_alerts で summary 連携を確認。 | 買い/売り/警戒/監視シグナルとして評論し、references不足時は背景補強の弱さを表現。 |
| H. Rule Lab / Pine | OK | live で strategy 作成、version 作成、Pine generate 成功、Pine regenerate 成功、revision input 保存を確認。 | 日本語自然文は PowerShell 経由だと文字化けし failed。警告・既知制約として記録。 |
| I. CSV import / Backtest | OK | `docs/34` runbook 参照。live で Performance Summary 英語 import 成功、invalid CSV failed、Backtest Detail の parsed / failed 共存を確認。 | BOM / Trades 英日 import は `test:e2e:backtest-import` 12件通過を根拠に OK。 |
| J. AI品質 | OK（直近確認済み） | `docs/35` と直近実データ確認で Home / Symbol / Comparison / Backtest / Alert を確認済み。 | `content空` / `reasoningのみ` / `length打ち切り` の再発は未確認。 |
| K. docs / secret | OK | `docs/32` / `docs/34` / `docs/35` / `docs/36` の参照導線あり。実値 secret 非記載。 | 一部 docs は shell 表示で文字化けして見えるが、主要runbookは実運用確認に耐える。 |

## 8. NG一覧
- なし

## 9. 要確認一覧
- Home の watchlist / positions カードに、追加した temp symbol が即時に視認できることは今回未再現。
  - 理由: card の表示件数制限、並び順、snapshot 有無の影響があり、API CRUD 成立と同義ではなかった。
  - 判定: `要確認` だが MVP blocker ではない。
- Rule Lab の日本語自然文を PowerShell 経由で投入した場合、文字化け経由で Pine generate failed になった。
  - 理由: shell / encoding 経路の影響。
  - 判定: UI入力では既存警告・直近確認あり。今回の shell 経路のみでは `要確認`。

## 10. MVP後に回す項目
- Performance Summary 日本語ヘッダー対応
- disclosure / earnings の供給改善
- references あり銘柄での追加 TradingView 実送信
- snapshot stale / market_status unknown 整理
- browser-based E2E 導入
- 複数ポートフォリオ UI
- transaction 履歴編集 UI
- 非日本銘柄の本格対応
- StrategyLab 文言テスト追加
- docs 全体の文字化け完全棚卸し

## 11. secret非露出確認
- `token` / `shared_secret` / `API key` / provider内部エラー全文の実値は docs に記載していない。
- webhook runbook は `<WEBHOOK_TOKEN>` / `<YOUR_SHARED_SECRET>` の placeholder のみ。
- 受入確認結果にも secret 実値は未記載。

## 12. 総合判定
- **OK**
- MVP 主要導線は、live API確認・既存実送信記録・runbook・関連testの組み合わせで受入可能水準にある。
- 現時点で MVP 完了を阻害する `NG` は見当たらない。

## 13. MVP完了可否
- **MVP完了可**
- 条件付きメモ:
  - `要確認` は残るが、いずれも既存導線の致命的不成立ではなく、MVP blocker ではない。
  - 主要既知課題は `docs/33` / `docs/35` / `docs/36` に記録済み。

## 14. 次に進めるべき作業
1. Home card の表示件数・並び順・snapshot 依存を短く仕様化し、watchlist / positions の「Home反映」の期待値を明文化する。  
2. Rule Lab の日本語入力を shell 以外の UI 経路で再確認し、文字化け警告の実利用上の見え方を記録する。  
3. MVP 完了後タスクとして、docs 全体の文字化け棚卸しと TradingView / references 系 runbook の統合整理を進める。  

## 15. 参照docs
- `docs/32.北極星 TradingView実送信 webhook運用手順（MVP）.md`
- `docs/33.北極星 references供給状況整理と運用課題（MVP）.md`
- `docs/34.北極星 TradingView CSV import 運用手順（MVP）.md`
- `docs/35.北極星 実データAI品質確認メモ（MVP）.md`
- `docs/36.北極星 MVP受入確認チェックリスト（MVP）.md`

