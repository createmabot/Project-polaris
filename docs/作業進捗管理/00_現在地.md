# 北極星 current status / progress entry

更新日: 2026-05-22
分類: 作業進捗管理

## 1. 目的

本資料は、作業進捗、完了整理、残課題、後続候補への入口である。詳細な積み上げ履歴は `docs/39` / `docs/44` / `docs/53` に残し、本資料では現在の読み方を整理する。

## 2. 現在の大枠

- P3 は完了扱い。
- UI foundation completion pass は完了済み。
- UI/UX production readiness phase 1 は、既存機能を増やさずに AppLayout / Navigation / SideRail / Home / SymbolDetail / StrategyLab の情報階層、余白、section grouping、主要 action / 補助情報の見え方を改善する範囲として完了扱いにする。API / DB / backend business logic は変更しない方針を維持する。
- UI/UX production readiness phase 2 は、ApplicationDetail / BacktestDetail / StrategyDetail / StrategyVersionDetail / BacktestComparisonDetail の検証・履歴系画面を対象に、AppLayout / PageHeader / SectionCard の適用、card density、loading / error / empty state、主要導線の見え方を改善する範囲として完了扱いにする。API / DB / backend business logic は変更しない方針を維持する。
- UI/UX production readiness phase 3 は、Phase 1 / Phase 2 で実際に重複した filter block、history card、proposal card、nested metadata、readable text block の surface 表現を `Surface` component として最小整理する範囲として完了扱いにする。DataTable 高機能化、modal framework 全面導入、visual regression required check 化は引き続き行わない。
- UI/UX production readiness acceptance smoke は、Phase 1 / 2 / 3 後の主要画面を browser smoke 観点で確認し、リリースを止める大きな UI 退行がないこと、proposal selection が自動 Pine / save / backtest / AI summary に連鎖しないこと、raw provider / path 系情報を出さないことを再確認した範囲として完了扱いにする。API / DB / Prisma schema / backend business logic は変更しない。
- IA / navigation debt cleanup は完了済み。
- Report comparison UX phase 2 は完了済み。
- AI summary auto-generation phase 1 は CSV import report / internal backtest report の auto enqueue まで完了済み。
- Visual regression pilot は optional check として最小導入済み。
- Artifact metadata / retention policy phase は、metadata 正本仕様、path 系 metadata 非表示、raw JSON sanitization、screen responsibility、retention / file access / diff boundary の整理まで完了済み。
- PR #337〜#340 の大フェーズ整理として、artifact file access / download permission boundary、AI summary comparison UX read-only helper、AI summary auto-generation phase 2 latest job status visibility、Visual regression optional pilot は完了扱いにする。
- Docs source-of-truth cleanup は完了済み。
- Legacy numbered docs cleanup は、PR #327〜#330 の削除・保留・移管先記録まで完了済み。
- 今回の docs architecture refactor では仕様書 / 運用ドキュメント / 作業進捗管理の3分類を導入する。
- Post-P3 release acceptance / stabilization は docs-only で整理済み。2026-05-21 の release / operations stabilization smoke で、Strategy proposal 完了後の主要導線、Backtest / report / artifact / AI summary 周辺、required / optional check 境界を再確認済み。完成範囲、known limitations、次期候補は `docs/運用ドキュメント/10_release_acceptance_checklist.md` を入口に確認する。
- AI quality / cost operations は、AI summary auto enqueue、latest job status visibility、Strategy proposal provider observation / provider event log が入った現在地を前提に、実装追加ではなく運用境界の設計整理として完了扱いにする。`stub` baseline、`local_llm` opt-in、real provider required check なし、failed job 自動 retry なし、polling / batch / scheduled なし、future `openai_api` は cost cap / rate limit / prompt length guard 設計後に判断、raw prompt / raw response / endpoint / model 実値 / secret / local path / stack trace 非表示の方針を維持する。
- Pine generation market / timeframe expansion は、Pine generation input / UI / warning / docs / tests の初回拡張として `JP_STOCK|US_STOCK` と `D|1D|4H|1H` までを完了範囲にする。internal backtest engine、market data provider、TradingView compile 自動実行、auto Pine / auto save / auto backtest / AI summary 自動生成は拡張しない。
- Daily operation UX stabilization は PR #343〜#346 の範囲で完了済み。SideRail の監視銘柄 / 保有銘柄操作、SymbolDetail references 手動再取得、TradingView CSV ファイル読込、既存 strategy 選択の密度改善までを完了扱いにする。
- LLM strategy proposal は、初回実装、PR #352〜#353 の provider boundary phase、PR #356〜#357 の local_llm provider implementation、PR #359〜#360 の quality evaluation phase、PR #362〜#363 の provider instrumentation phase、PR #365〜#366 の prompt regression / provider benchmark phase、PR #368〜#370 の proposal history / selected proposal lineage phase、provider quality trend aggregation、benchmark result recording workflow / sanitized summary records、provider cost / rate guard hardening、Codex CLI manual JSON import、Codex CLI Web検索付き prompt option、proposal history full management、sanitized provider event log persistence、proposal history soft archive まで完了済み。`STRATEGY_PROPOSAL_PROVIDER=stub|local_llm`、default `stub`、local_llm opt-in、既存 schema validation pipeline、malformed JSON / invalid schema / unavailable / timeout の provider error、quality evaluation runbook、validation / failure path test 拡充、`provider_observation` optional response metadata、StrategyLab の最小 provider note / error note、fixed scenario set、code fixture、optional benchmark script、`--output` による gitignore 済み sanitized summary record、`StrategyProposalRun` / `StrategyProposalCandidate` による sanitized generation run / candidates 保存、recent list / detail / selection API、StrategyLab の compact proposal history filter / pagination / search / soft archive UI、sanitized history からの provider quality trend read-only 集計、local_llm timeout / max output の bounded env guard、in-memory per-process rate guard、429 rate limited UI message、`codex_cli_manual` の prompt生成 / JSON貼り付け・file text import / history保存、Web検索付き prompt guidance、`StrategyProposalProviderEvent` による sanitized provider event 保存までを完了扱いにする。投資助言風 wording は入力段階でも provider output 段階でも、それだけを理由に reject せず、検証候補であることを UI / docs / benchmark / trend note で明示する。

## 3. progress docs の役割

- `docs/39.北極星 MVP後ロードマップ・バックログ整理.md`: MVP後ロードマップと backlog の履歴。
- `docs/44.北極星 MVP後P2完了整理とP3着手判断.md`: P2完了 / P3着手判断と後続追記。
- `docs/53.北極星 P3現在地と残課題整理（P3）.md`: P3完了後も含む現在地と残課題の集約正本。
- `docs/作業進捗管理/00_現在地.md`: 次回作業時に progress docs の読み方を示す入口。
- `docs/作業進捗管理/01_ロードマップ.md`: フェーズ別ロードマップと次候補。
- `docs/作業進捗管理/02_完了フェーズ.md`: 完了扱いにした範囲の要約。
- `docs/作業進捗管理/03_残課題_Backlog.md`: 後続判断として残す課題の集約。
- `docs/作業進捗管理/04_設計判断ログ.md`: 横断的な設計判断と見送り理由。
- `docs/作業進捗管理/05_docs整理履歴.md`: docs 整理の履歴と更新ルール。
- `docs/作業進捗管理/06_番号付きdocs削除整理.md`: legacy numbered docs cleanup の削除済み一覧、移管先、保留理由、後続判断。
- `docs/作業進捗管理/07_AI_summary自動生成phase1完了.md`: AI summary auto-generation phase 1 の完了範囲、対象外、次候補。

## 4. 次の大きな候補

- legacy numbered docs cleanup の後続として、data / API 系残件、Rule Lab 系、MVP受入記録、progress 詳細 docs の統合可否を判断する。
- API sample docs と現行 routes / tests の対応整理。
- walkthrough の肥大化対策と確認観点別分割。
- Visual regression pilot の実行負荷と snapshot churn を見て、本導入可否を再判断する。
- AI summary auto enqueue 後続として、retry / polling / live status update、batch / scheduled job、provider cost cap / rate limit / opt-in policy は継続見送りまたは追加設計が必要な候補として扱う。現時点では実装しない。
- AI summary comparison 後続として、必要な場合のみ comparison route / entity、本格比較文生成、metrics normalization を判断する。
- Artifact 後続として、download / signed URL / file token / backend proxy の本格設計、artifact diff / JSON diff、retention job を判断する。
- Daily operation UX stabilization 後続として、external symbol metadata provider、references refresh job/status history UX、実サンプルに基づく CSV parser alias 拡張、strategy version picker の検索 / pagination、daily ops browser smoke 拡張を判断する。
- LLM strategy proposal 後続は、1. StrategyVersion created-from-proposal relation、2. proposal history retention / hard delete / export、3. openai_api provider、4. Web search / deep research job 化、5. Codex CLI local worker / automatic spawn の継続見送り判断、6. provider event log based quality trend upgrade、7. prompt regression automation、8. benchmark result DB table、9. provider quality trend の range / filter / percentile / materialized aggregation、10. distributed rate limit / hard cost cap / per-user billing に整理する。auto Pine generation / auto save は引き続き対象外とする。
- Proposal history soft archive は完了済みである。`StrategyProposalRun.archivedAt` の additive nullable field、default active list、`archived=active|archived|all` filter、archive / unarchive action、archived detail / selection 許可、provider event log とは独立、hard delete / retention / export 対象外を完了境界とする。
- UI/UX production readiness acceptance smoke は完了済みである。今後 UI follow-up を扱う場合は、実際の重複や運用上の違和感を起点に小さく設計し、DataTable 高機能化、modal framework 全面導入、visual regression required check 化は引き続き個別判断とする。
- Pine generation market / timeframe expansion は初回 scope 完了済みである。今後 TradingView / Pine workflow を広げる場合は、internal backtest engine の timeframe / market data provider 対応、FX / CRYPTO、15M / 30M、real TradingView compile 自動実行をそれぞれ別設計に分ける。

優先度:

1. Release / operations stabilization。
2. AI quality / cost operations は docs-only 設計整理まで完了済み。次に実装へ進む場合は、`openai_api` provider、distributed rate limit / hard cost cap、scheduled / batch AI summary、polling / live update、provider event log based quality trend upgrade をそれぞれ別設計に分ける。
3. LLM strategy proposal provider operations / openai_api expansion。
4. Artifact operations phase 2。
5. Report comparison phase 3。
6. Performance / scale。

Visual regression expansion と TradingView / Pine workflow 強化は継続候補だが、optional pilot と現行 Pine workflow の運用観測後に優先度を再評価する。UI/UX production readiness の後続は、実際に重複が増えた場合のみ DataList / SimpleTable / DataTable を小さく検討する。高機能 DataTable、modal framework 全面導入、visual regression required check 化は引き続き後続判断とする。

## 5. 今回やらないこと

- 実装コード変更。
- DB / Prisma schema / API / test 変更。
- docs大量削除 / 大量rename。
- 旧docsの全面書き換え。
