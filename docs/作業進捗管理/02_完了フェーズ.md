# 北極星 完了フェーズ

更新日: 2026-05-13
分類: 作業進捗管理

## 1. 目的

本資料は、完了扱いにしたフェーズとその範囲を短く確認するための資料である。詳細な履歴は `docs/39`、`docs/44`、`docs/53` を参照する。

## 2. MVP 完了

完了扱い:

- Rule Lab の自然言語ルール入力。
- Strategy / Strategy Version 作成。
- Pine 生成と修正再生成。
- TradingView CSV import。
- Backtest Detail 表示。
- Backtest AI summary 手動生成。
- inline comparison / saved pairwise comparison。
- Home / SymbolDetail / Comparison の最小 API flow。

完了判断は `docs/36`、`docs/37`、`docs/38` を履歴として参照する。

## 3. MVP後 P1 / P2 完了

P1 / P2 では、MVP直後の運用品質と既知課題の整理を完了扱いにした。

主な完了範囲:

- docs文字化け棚卸し範囲整理。
- Rule Lab 日本語入力の UI 経路再確認。
- snapshot stale / `market_status=unknown` の扱い整理。
- TradingView CSV import の日本語ヘッダー対応方針。
- references 供給状況と TDnet diagnostics の見方整理。
- browser-based E2E PoC の方針固定。

詳細は `docs/39` と `docs/44` を参照する。

## 4. P3 完了

P3 は、Home / SideRail から SymbolDetail、Symbol Strategy Application、CSV import report、internal backtest report、BacktestDetail 確認までを一連の利用フローとして辿れる状態を完了価値として扱う。

主な完了範囲:

- AppLayout / PageHeader / Navigation / TextLink / SectionCard の最小 UI 土台。
- Home / SideRail の表示整理と refresh optimization。
- StrategyList / StrategyDetail の read-only 表示、archive / restore。
- Symbol Strategy Application の DB / API / UI 接続。
- CSV import wiring。
- internal backtest wiring と report conversion。
- BacktestDetail backlink / importless report 表示。
- read-only browser smoke。

詳細は `docs/53.北極星 P3現在地と残課題整理（P3）.md` を正本とする。

## 5. P3後続の完了扱い

P3後続では、既存機能を壊さない小さな整理を積み上げている。

完了扱い:

- Application Detail / History foundation + usability pass。
- UI cleanup second pass。
- UI foundation third pass。
- Report comparison UX phase 2。
- AI summary / artifact operations phase。
- UI foundation completion pass。
- AI summary auto-generation operations design と CSV / internal report auto enqueue 方針整理。
- AI summary auto-generation phase 1。CSV import parsed report、application 起点 CSV import、internal backtest report conversion の auto enqueue、duplicate guard、manual generation 維持、BacktestDetail / ApplicationDetail read-only visibility 整理までを完了扱いにする。
- docs source-of-truth cleanup。
- docs architecture refactor の仕様書側初回整理。

## 6. 完了扱いに含めないもの

- hard delete。
- favorite / last used / display priority などの richer metadata。
- metrics normalization table。
- full comparison screen。
- artifact file read / download / diff。
- Visual regression 本導入。
- page view 起点の AI summary enqueue。
- DB / Prisma schema を伴う大きな再設計。

## 7. 関連 docs

- `docs/38.北極星 MVP完了報告（MVP）.md`
- `docs/44.北極星 MVP後P2完了整理とP3着手判断.md`
- `docs/53.北極星 P3現在地と残課題整理（P3）.md`
- `docs/作業進捗管理/03_残課題_Backlog.md`
- `docs/作業進捗管理/07_AI_summary自動生成phase1完了.md`
