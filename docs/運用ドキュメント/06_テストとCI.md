# 北極星 テストと CI

更新日: 2026-05-15
分類: 運用ドキュメント

## 1. 目的

本資料は、北極星で使う test / CI の日常運用をまとめる。仕様としてのテスト方針は `docs/仕様書/10_テスト仕様.md`、browser-based E2E の背景は `docs/45.北極星 browser-based E2E導入方針（P3）.md` を参照する。

## 2. テストの役割分担

- backend API E2E: API 契約、保存導線、queue / collector / summary flow を固定する。
- frontend Vitest: component 文言、空状態、URL helper、表示分岐を固定する。
- frontend flow test: return-flow や URL query の契約を固定する。
- Playwright browser smoke: 実ブラウザで主要 read-only 導線が成立することを確認する。
- Playwright visual pilot: 任意実行の screenshot comparison で、少数の安定 container だけを確認する。
- manual walkthrough: TradingView、CSV file 選択、外部ツール連携など、自動化しすぎない確認を担う。

## 3. よく使うコマンド

```bash
pnpm run build
pnpm run test
pnpm --filter backend test:e2e:home-symbol-comparison
pnpm --filter backend test -- test/backtest-ai-summary.e2e.test.ts
pnpm --filter frontend test:e2e:browser
pnpm test:e2e:browser
pnpm test:e2e:visual
git diff --check
```

作業範囲が小さい場合は、該当 package / spec に絞って実行する。docs-only PR では build / test を無理に実行せず、`git diff --check` と docs scan を優先する。

AI summary auto-generation phase 1 の route 別確認観点は `docs/運用ドキュメント/08_AI_summary自動生成運用.md` を参照する。docs-only の整理では `git diff --check` を最低限確認し、CI required check や新規 test の追加は行わない。

Strategy proposal benchmark は manual / optional として扱う。`pnpm --filter backend strategy-proposal:benchmark` と `--output=<file>.json` は required check に入れず、real `local_llm` provider 依存 benchmark も CI required check にしない。CI で確認する場合は stub / fake data による sanitizer、schema、output path validation に限定する。

## 4. Browser smoke の前提

Playwright smoke は local-only から始める。

前提:

- PostgreSQL / Redis 起動済み
- backend 起動済み
- frontend 起動済み
- migration / seed 済み
- 外部 API / LLM / TradingView に依存しない導線を優先する

初回 browser install が必要な場合だけ次を実行する。

```bash
pnpm --filter frontend exec playwright install chromium
```

## 5. Visual regression pilot

Visual regression pilot は optional check として扱う。`pnpm test:e2e:browser` には含めず、CI required check にも追加しない。

対象:

- `ApplicationDetail` の `application summary` container 1 箇所。
- seed 固定 ID の read-only 画面を使う。
- dynamic timestamp / locale 表示は mask する。
- raw JSON、AI 生成文、TradingView widget、long page 全体 screenshot は対象外。

通常確認:

```bash
pnpm test:e2e:visual
```

snapshot 更新:

```bash
pnpm --filter frontend test:e2e:visual -- --update-snapshots
```

更新後は差分画像が最小枚数であること、意図しない UI 変更を含まないこと、`pnpm test:e2e:browser` に visual spec が混ざっていないことを確認する。

## 6. CI required checks

GitHub Actions では次の checks を運用する。

- `strategy-versions-return-flow-e2e-check`
- `backtests-return-flow-e2e-check`
- `snapshot-review-generator-json-check`
- `symbol-snapshot-db-integration`

`main` の required checks は ruleset で管理する。check 名と workflow 名がずれると `Expected - Waiting for status to be reported` になるため、ruleset と実際の check 名を合わせる。

## 7. Required-check failure drill

required check failure 時に merge が block されることを定期確認する。四半期に 1 回、または ruleset / branch protection 変更時に実施する。

最小手順:

1. `main` から検証ブランチを作成する。
2. 対象 check が落ちる最小の一時破壊を入れる。
3. PR を作成し、対象 check が failure になることを確認する。
4. PR が required check 未通過で block されることを確認する。
5. 一時破壊を revert し、success に戻ることを確認する。
6. 結果を snapshot weekly review の運用記録へ残す。

検証用の破壊コミットは `main` に merge しない。

## 8. docs-only PR の確認

- `git diff --check` を実行する。
- release acceptance / stabilization の docs-only PR では `docs/運用ドキュメント/10_release_acceptance_checklist.md` の主要導線、完成範囲、known limitations、次期候補が README / `docs/0` / `docs/57` / walkthrough と矛盾しないことを確認する。
- 変更 docs に local path、secret、token、shared_secret、API key の実値がないことを scan する。
- 実装コード、API、backend、frontend、DB、Prisma schema、tests を変更していないことを確認する。
- PR 本文は ASCII English only にする。

## 9. 関連 docs

- `docs/仕様書/10_テスト仕様.md`
- `docs/45.北極星 browser-based E2E導入方針（P3）.md`
- `docs/運用ドキュメント/08_AI_summary自動生成運用.md`
- `docs/運用ドキュメント/10_release_acceptance_checklist.md`
- `docs/walkthrough.md`
- `docs/snapshot-weekly-review-record-template.md`
