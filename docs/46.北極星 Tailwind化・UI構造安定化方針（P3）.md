# 北極星 Tailwind化・UI構造安定化方針（P3）

更新日: 2026-05-09

## 1. 目的

- Tailwind 化の目的を、見た目刷新ではなく保守性向上として整理する
- UI 構造安定化の対象範囲と順序を固定する
- 画面導線 / IA / ナビゲーション変更と Tailwind 化の前後関係を明確にする
- `docs/45` の browser-based E2E 方針と矛盾しない frontend test 方針を維持する
- `docs/47` の画面導線 / IA / ナビゲーション方針に沿って、触る画面から段階移行する

本ドキュメントで固定する前提は次のとおり。

- 全画面を先に Tailwind 化してから導線変更する方針は採らない
- Tailwind 設定追加は先に進めてよい
- Home / SymbolDetail など画面単位の Tailwind 移行は、画面導線 / IA / ナビゲーション方針整理の後に行う
- 共通 UI component は、導線変更で実際に触る画面から段階的に抽出する

## 2. 現行UI課題

現行 frontend では、特に次の課題がある。

- inline style が多い
- 画面ごとに余白、外枠、section card、button、戻るリンク行の実装が重複している
- `Home` `SymbolDetail` `ComparisonDetail` `StrategyLab` `BacktestDetail` `StrategyVersionDetail` で card / section / link / button 表現が揺れている
- empty / loading / error state の構造が画面ごとに揺れている
- 画面導線やナビゲーションの見直しが入る前に内部 component を抽出しすぎると、あとで分解し直す可能性が高い

補足:

- `Home` と `SymbolDetail` は MVP 入口導線として重要で、今後も画面構造の整理対象になりやすい
- `StrategyLab` `BacktestDetail` `ComparisonDetail` `StrategyVersionDetail` は状態分岐が多く、初回の UI 構造安定化対象としては重い

## 3. Tailwind 化の目的

Tailwind 化の目的は次のとおり。

- UI の一貫性向上
- inline style 削減
- 画面改修速度向上
- card / button / section / layout の統一
- 将来の responsive 対応と見た目改善の土台作り

ただし、今回の基準点は「見た目の全面刷新」ではない。

優先するのは次の順序である。

1. Tailwind 設定を入れられる状態にする
2. 画面導線 / IA / ナビゲーション方針を整理する
3. その導線変更で触る画面から Tailwind 化と共通 component 化を進める

## 4. 対象画面比較

| 候補 | 価値 | コスト | リスク | 初回向きか |
|---|---:|---:|---:|---|
| Home | 高 | 中 | 中 | 導線方針整理後なら最初に向く |
| SymbolDetail | 高 | 中 | 中 | Home の次に向く |
| Watchlist / Positions | 中 | 低〜中 | 低 | Home / SymbolDetail 後に向く |
| StrategyLab | 高 | 中〜高 | 高 | まだ早い |
| BacktestDetail | 高 | 高 | 高 | まだ早い |
| ComparisonDetail | 中〜高 | 中〜高 | 中〜高 | まだ早い |
| StrategyVersionDetail | 中 | 高 | 高 | まだ早い |

判断:

- 初回の画面単位移行候補は引き続き `Home` だが、着手前に導線方針整理を挟む
- `SymbolDetail` は `Home` に続く導線先として次点
- `Watchlist / Positions` は CRUD 管理画面として共通 form/list の効果が高いが、入口導線よりは後
- 状態分岐の多い画面は、共通土台ができるまで後回しにする

## 5. 初回移行対象

推奨:

- 画面単位の初回移行対象は `Home`

ただし、前提条件を置く。

- `Home` の Tailwind 移行は、画面導線 / IA / ナビゲーション方針整理の後に行う
- `Home` 全面移行を Tailwind 設定追加 PR と同時には行わない

理由:

- `Home` は MVP 入口であり、既存 Playwright PoC の対象でもある
- card / section / link / button の共通化効果が高い
- 一方で、導線変更予定がある状態で先に `SectionCard` などを大量抽出すると、後から再設計コストが出る

## 6. 共通UI component方針

### 6.1 先に検討する候補

導線方針整理の前後をまたいでも比較的壊れにくい最小候補は次のとおり。

- `AppLayout`
- `PageHeader`
- `Navigation`
- `TextLink`

これらは、ページ外枠、戻る導線、上位ナビゲーション、共通リンク表現として先に整理しやすい。

### 6.2 導線変更後に抽出する候補

画面内部構造に強く依存するものは、導線変更後の構造が固まってから抽出する。

- `SectionCard`
- `EmptyState`
- `LoadingState`
- `ErrorState`
- `PrimaryButton`
- `SecondaryButton`
- `DataList / KeyValueRow`
- `StatusBadge`
- `AlertSummaryCard`
- `AiSummaryCard`
- `ReferenceBreakdown`

理由:

- これらは画面内部の section 境界や情報階層の影響を受けやすい
- 導線変更前に抽出すると、抽象化が早すぎて逆に手戻りが増える

### 6.3 方針

- 最初から全部共通化しない
- 導線変更で触る画面から、実際に重複が確認できたものだけを抽出する
- 過剰抽象化を避ける

## 7. Tailwind導入方式

### 案1: Tailwind を導入し、既存画面を段階的に `className` 化

- 推奨
- 差分を小さく分けやすい
- Tailwind 設定追加 PR と画面移行 PR を分離しやすい

### 案2: 共通 component を先に作り、その中だけ Tailwind を使う

- 一部有効
- ただし、導線変更前にやりすぎると抽象化コストが高い

### 案3: 画面全面を一気に Tailwind 化

- 非推奨
- 差分が大きく、導線変更予定と競合しやすい
- test 調整と画面確認のコストが上がる

推奨方針:

- Tailwind 導入 PR と Home 移行 PR は分ける
- まず Tailwind 設定追加のみ、または Tailwind 設定追加 + 最小 global CSS 整理まで
- その後に画面導線 / IA / ナビゲーション方針整理を行う
- 画面単位移行は、その導線変更で触る画面から始める

## 8. Tailwind導入順序

推奨順:

1. Tailwind 設定追加のみ、または Tailwind 設定追加 + 最小 global CSS 整理
2. 画面導線 / IA / ナビゲーション方針整理
3. 導線変更で最初に触る画面向けに `AppLayout` / `PageHeader` / `Navigation` / `TextLink` を最小導入
4. `Home` の最小置換
5. `Playwright` / `Vitest` / build 確認
6. `SymbolDetail` へ拡張
7. `Watchlist / Positions` へ拡張
8. `StrategyLab` / `BacktestDetail` / `ComparisonDetail` / `StrategyVersionDetail` は後続判断

明示する判断:

- 「全画面 Tailwind 化してから導線変更」は採らない
- `Home` 全面移行はまだ行わない
- 2026-05-09 時点で Tailwind 設定追加と最小 global CSS 整理までは完了
- 2026-05-09 時点で `AppLayout` / `PageHeader` / `Navigation` / `TextLink` の最小土台も `Home` / `SymbolDetail` にだけ適用済み
- `SideRail` の最小 PoC は `Home` / `SymbolDetail` にだけ追加済み
- `SideRail` は `/api/home` 再利用、タブ切り替え、折りたたみ、`/watchlist` `/positions` への補助導線までに留める
- `SideRail` の CRUD モーダル、`SectionCard` など画面内部 component は未実装
- 2026-05-09 時点で `Home` の最小移行は完了し、`Home` 本体から watchlist / positions の詳細一覧を外して `SideRail` に役割を寄せた
- 2026-05-09 時点で `SideRail` の CRUD モーダル化も最小実装まで完了した
- 次 PR は `SymbolDetail` の最小移行、または `SideRail` 再取得最適化判断へ進む

## 9. テスト方針

`docs/45` の方針を引き継ぐ。

- Tailwind class を test しない
- layout / color / spacing を test しない
- role / heading / link / button / URL / stable text を見る
- Playwright は実ブラウザ導線確認に限定する
- Vitest は文言、空状態、helper、URL 生成、props 分岐を維持する
- backend E2E は API 契約と保存導線を担保する

追加判断:

- 既存 `Home → SymbolDetail` Playwright PoC は維持する
- 導線変更が入った場合は、PoC spec を必要最小限で調整する
- 2 本目以降の Playwright spec 追加は、Tailwind 化または UI 構造安定化後に判断する
- Visual regression / screenshot comparison はまだ入れない

## 10. 今回やらないこと

- Tailwind 導入
- CSS 改修
- UI 実装
- component 作成
- Playwright spec 追加
- Visual regression
- CI 導入
- API 改修
- DB 改修
- backend 改修

## 11. 次PR候補

候補1:

- 画面導線 / IA / ナビゲーション方針整理 (`docs/47`)

候補2:

- `AppLayout` / `PageHeader` / `Navigation` / `TextLink` の最小土台追加

候補3:

- `Home` の最小置換

補足:

- `Home` の `SectionCard` など画面内部 component 抽出は、導線方針整理の後に回す
- `Home` 全面移行は、設定追加 PR の次ターンでもまだ行わない

推奨:

- 次 PR は `Home` の最小移行、または `SideRail` PoC の優先度判断
- その後に、導線変更で最初に触る画面の内部構造調整へ進む

## 12. 判断

- Tailwind 化の次アクションとして、設定追加と最小 global CSS 整理までは完了した
- `AppLayout` / `PageHeader` / `Navigation` / `TextLink` の最小土台も完了した
- ただし画面単位の Tailwind 移行は、導線変更予定を踏まえて後ろに置く
- 共通 UI component は、導線変更で触る画面から順に最小抽出する
- `docs/45` の browser-based E2E 方針と矛盾しないよう、PoC 拡張は UI 構造安定後に判断する

## 追記（2026-05-09）

- `SymbolDetail` の最小移行を完了した。
- `現在スナップショット` `最新アラート` `最新AI論点カード` `Research Note` `関連参照情報` の section を最小 Tailwind 化し、`SideRail` と共存する画面構造に整理した。
- 次 PR は銘柄起点ストラテジー適用フロー設計、または `SideRail` 再取得最適化判断へ進む。

## 追記（2026-05-09 その2）

- 銘柄起点ストラテジー適用フロー設計を [docs/48.北極星 銘柄起点ストラテジー適用フロー設計（P3）.md](./48.北極星%20銘柄起点ストラテジー適用フロー設計（P3）.md) に固定した。
- `SymbolDetail` の次段階は、`ストラテジー / 検証結果` 空 section を追加して、銘柄起点導線の受け皿だけ先に整える。
- `StrategyList` / `StrategyDetail` の route 実装や DB 保存概念の確定は後続で判断する。

## 追記（2026-05-09 その3）

- `SymbolDetail` に銘柄起点ストラテジー適用の受け皿として `ストラテジー / 検証結果` section を追加した。
- 既存 section や `SideRail` との共存を維持したまま、補助導線だけ先に可視化した。

## 追記（2026-05-09 その4）

- `StrategyList` placeholder route を追加し、再利用可能な Strategy Definition の受け皿を先に可視化した。
- 既存 `StrategyLab` は作成 / 生成入口、`BacktestList` / `BacktestDetail` は検証レポート一覧 / 詳細として継続し、役割分離は維持している。

## 追記（2026-05-10 その4）

- UI構造安定化の一環として SideRail refresh optimization を追加した。
- AppLayout から SideRail へ Home latest data / mutate を渡せるようにし、Home 表示時の同一 home data 取得を共有する。
- SideRail の見た目や CRUD 仕様は大きく変更せず、data fetch / mutate 方針だけを整理した。

## 追記（2026-05-10 その5）

- UI構造安定化として `SectionCard` を追加した。
- Home / SymbolDetail の section wrapper を最小置換し、Tailwind の section/card 表現を共通 component に寄せ始めた。
- 全面 Tailwind 化や大規模 redesign は行っていない。

## 追記（2026-05-10 その6）

- UI 構造安定化の確認として、P3 read-only navigation scenario を Playwright に追加した。
- Home / SymbolDetail / StrategyDetail / BacktestDetail の主要 section と backlink 導線を、実行操作なしで確認する。
- UI component cleanup は段階的に継続し、今回の scenario では見た目刷新や API 変更を行わない。

## 追記（2026-05-10 その7）

- UI component cleanup の小さな継続として、`SymbolDetail` の Strategy Application row 表示を整理した。
- `SectionCard` の適用範囲は広げず、`SymbolDetail` 内 private component で summary / latest run / latest report を分割した。
- 表示内容、導線、API 呼び出し、archive / restore 操作は変更していない。

## 追記（2026-05-10）

- UI component cleanup の小さな継続として、`BacktestDetail` の backlink 表示を整理した。
- 全面 Tailwind 化や共通 component の過剰抽出は行わず、`銘柄起点の適用情報` のカード表示を BacktestDetail 内の小さな private component に切り出した。

## 追記（2026-05-10）

- UI 構造安定化の現在地は [[53.北極星 P3現在地と残課題整理（P3）]] に集約した。
- `SectionCard` の限定適用と SymbolDetail / BacktestDetail 内 private component 分割は完了扱いとし、Button / EmptyState / LoadingState の共通化は次段階の判断事項とする。

## 追記（2026-05-10）

- Tailwind化 / UI構造安定化の残タスクを [[53.北極星 P3現在地と残課題整理（P3）]] に補足した。
- 現在の Tailwind化は土台実装と導線安定化が中心であり、見た目刷新完了ではない。UX改善目的の余白、情報密度、状態表示、導線ラベル、responsive 対応は後続タスクとして残す。
- 全画面一括 Tailwind 化は行わず、Playwright smoke と既存導線を壊さない範囲で section 単位に進める。

## 追記（2026-05-10）

- P3完了後の UI / UX cleanup として、`EmptyState` / `ErrorState` の最小 component を追加した。
- 適用先は `StrategyDetail` の一部状態表示に限定し、画面固有文言と導線は呼び出し側に残す。
- LoadingState / Button / StatusBadge などの共通化は後続判断とする。

## 追記（2026-05-10）

- `EmptyState` / `ErrorState` の限定適用を `BacktestDetail` に広げた。
- 取込状態、主要指標、AI総評などの empty / error 表示を小さく揃え、BacktestDetail の全面 redesign は行わない。
- LoadingState / Button / StatusBadge / KeyValueRow / DataList の共通化は引き続き後続判断とする。

## 追記（2026-05-10）

- `EmptyState` / `ErrorState` の限定適用を `SymbolDetail` に広げた。
- 最新アラート、保存済み application、Research Note などの empty / error 表示を小さく揃えた。
- CSV import / internal backtest / report conversion の操作状態や仕様は変更しない。

## 追記（2026-05-10）

- UI / UX cleanup として `LoadingState` を追加した。
- まず `StrategyDetail` / `BacktestDetail` の loading 表示へ限定適用し、画面固有文言は呼び出し側に残す。
- Button / StatusBadge / KeyValueRow / DataList の追加や全面 redesign は引き続き行わない。

## 追記（2026-05-10）

- 状態表示 component 第一段階として、`EmptyState` / `ErrorState` / `LoadingState` の導入を完了扱いにする。
- 適用済みは `StrategyDetail` / `BacktestDetail` / `SymbolDetail` の一部であり、`Home` / `SideRail` / `StrategyVersionDetail` / `StrategyLab` / `BacktestComparisonDetail` は未対応として残す。
- 次の UI cleanup は `Button`、次に `StatusBadge`、その後 `KeyValueRow` / `DataList` の順で検討する。Visual regression はまだ導入しない。

## 追記（2026-05-10）

- UI / UX cleanup として `Button` component の最小導入を開始した。
- 適用先は `StrategyDetail` の archive / restore 操作ボタンに限定し、リンク表現や `SymbolDetail` / `BacktestDetail` の操作ボタンは変更しない。
- `StatusBadge` / `KeyValueRow` / `DataList`、Visual regression は引き続き後続判断とする。

## 追記（2026-05-10）

- `Button` component の限定適用を `SymbolDetail` に広げた。
- 保存済み application の archive、適用保存、AI論点カード生成 / 再生成の操作ボタンを対象にし、CSV import / internal backtest / report conversion の実行ボタンは変更しない。
- `StatusBadge` / `KeyValueRow` / `DataList`、Visual regression は引き続き後続判断とする。

## 追記（2026-05-11）

- UI / UX cleanup として `StatusBadge` component の最小導入を開始した。
- `StrategyDetail` と `SymbolDetail` の一部 status 表示に限定適用し、status enum の大規模整理や画面 redesign は行わない。
- `KeyValueRow` / `DataList`、Visual regression は引き続き後続判断とする。

## 追記（2026-05-11）

- `StatusBadge` component の限定適用を `BacktestDetail` に広げた。
- Backtest report、CSV import、Symbol Strategy Application backlink の status 表示を対象にし、比較結果や AI summary 生成状態には広げない。
- `KeyValueRow` / `DataList`、Visual regression は引き続き後続判断とする。

## 追記（2026-05-11）

- UI / UX cleanup として `KeyValueRow` / `KeyValueList` component の最小導入を開始した。
- `BacktestDetail` の一部 key-value 表示に限定適用し、JSON dump、artifact raw 表示、画面全体 redesign には広げない。
- Visual regression / screenshot comparison は引き続き後続判断とする。

## 追記（2026-05-11）

- UI共通 component 第一巡として、`EmptyState` / `ErrorState` / `LoadingState` / `Button` / `StatusBadge` / `KeyValueList` / `KeyValueRow` の最小導入を完了扱いにする。
- 適用済みは `StrategyDetail` / `SymbolDetail` / `BacktestDetail` の一部に限定し、`Home` / `SideRail` / `StrategyVersionDetail` / `StrategyLab` / `BacktestComparisonDetail` は後続判断として残す。
- JSON viewer、DataTable、form input、modal、Visual regression は第一巡では導入しない。

## 追記（2026-05-11）

- `SymbolDetail` の保存済み application / run 表示に `KeyValueList` / `KeyValueRow` / `StatusBadge` を限定適用した。
- 全面 redesign ではなく、既存導線と既存操作を保ったまま情報階層を整える方針を維持する。
- Home / SideRail、未対応画面、Visual regression は引き続き後続判断とする。

## 追記（2026-05-12）StrategyVersionDetail cleanup 段階1

- UI cleanup second pass の段階1として、`StrategyVersionDetail` の状態表示と概要系表示を小さく整理した。
- `LoadingState` / `ErrorState` / `EmptyState` / `SectionCard` / `Button` / `StatusBadge` / `KeyValueList` / `KeyValueRow` / `TextLink` を限定適用し、基本情報、Pine 状態、lineage、forward validation note、warnings / assumptions、generated pine の見通しを揃えた。
- Pine generation / regenerate / validation / backtest / application の API 呼び出し、導線、挙動は変更していない。
- StrategyLab / BacktestComparisonDetail / Home / SideRail、form / modal / table component 本格共通化、Visual regression は後続判断として残す。

## 追記（2026-05-12）StrategyLab + BacktestComparisonDetail cleanup 段階2

- UI cleanup second pass の段階2として、`StrategyLab` と `BacktestComparisonDetail` の状態表示と概要表示を小さく整理した。
- `SectionCard` / `EmptyState` / `ErrorState` / `LoadingState` / `Button` / `StatusBadge` / `KeyValueList` / `KeyValueRow` / `TextLink` を限定適用し、入力、生成結果、CSV取込、comparison summary、report pair、metrics の見通しを揃えた。
- StrategyLab の生成・保存・検証、Backtest comparison の API shape と仕様は変更していない。
- Home / SideRail、form / modal / table component 本格共通化、Visual regression は後続判断として残す。

## 追記（2026-05-12）Home / SideRail light cleanup 段階3

- UI cleanup second pass の段階3として、`Home` と `SideRail` の軽い状態表示整理を完了扱いにする。
- `LoadingState` / `ErrorState` / `EmptyState` / `Button` / `KeyValueList` / `KeyValueRow` を限定適用し、Home の loading / error / empty、マーケット概況、SideRail の empty / loading / error と軽い操作ボタン表示を揃えた。
- Home / SideRail の `/api/home` 共有、watchlist / positions の SWR key、mutate 範囲、CRUD 挙動は変更していない。
- form / modal / table component 本格共通化、Visual regression は後続判断として残す。

## 追記（2026-05-12）UI cleanup second pass 現在地

- UI cleanup second pass は `StrategyVersionDetail`、`StrategyLab`、`BacktestComparisonDetail`、`Home`、`SideRail` の限定 cleanup までを完了扱いにする。
- 完了範囲は section / status / key-value / empty / loading / error / selected button の小さな整理であり、refresh / CRUD / API / backend / DB / Prisma schema は変更していない。
- form input / textarea / select / modal shell / table / data list / JSON viewer の本格共通化は今回は見送る。画面ごとの form semantics、confirm / destructive action、表示密度、artifact / snapshot / AI context の用途差を整理してから判断する。
- Visual regression / screenshot comparison は今回は導入せず、seed data、dynamic timestamp / locale 表示、state fixture、viewport、CI runtime を安定させた後の後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR1: form component 方針更新

- form input / textarea / select は、最小 props の薄い UI component として段階導入を開始した。
- `label` / `helpText` / `error` は component 側で薄く描画し、文言、validation、API 呼び出しは呼び出し側に残す。
- 初回適用は StrategyLab と StrategyVersionDetail の一部に限定し、全画面一括適用や form framework 導入は行わない。
- modal shell、table / data list、JSON viewer、Visual regression は引き続き後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR2: modal shell 方針更新

- modal shell は、既存 `Modal` の挙動を維持した薄い wrapper として段階導入を開始した。
- 初回適用は SideRail の watchlist / positions CRUD modal に限定し、title / children / actions / onClose の整理に留める。
- destructive action の文言、confirm 内容、submit / delete handler、refresh / API 呼び出しは呼び出し側に残す。
- keyboard trap の大規模実装、table / data list、JSON viewer、Visual regression は引き続き後続判断とする。

## 追記（2026-05-12）UI foundation third pass PR3: data display 方針更新

- data list / simple table / JSON block 系の初回整理として、`JsonBlock` のみを最小導入した。
- 初回適用は BacktestDetail の raw artifact JSON に限定し、collapsible、copy button、JSON diff、artifact diff は実装しない。
- ApplicationDetail / BacktestComparisonDetail の list / metrics 表示は既存 component で意図が保てているため、RecordList / SimpleTable / DataTable 抽出は見送る。
- 本格 DataTable、sorting、selection、virtual scroll、column resize、Visual regression は引き続き後続判断とする。
