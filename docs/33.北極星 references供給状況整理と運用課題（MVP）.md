# 北極星 references供給状況整理と運用課題（MVP）
更新日: 2026-05-08

## 1. 目的

本資料は、実データ運用時の `external_references` 供給状況を観測し、`news / disclosure / earnings` の偏り、0件時の画面挙動、AI要約への影響、運用上の確認手順を固定するための補助資料である。

今回の対象は観測と整理であり、references 収集ロジックの全面改修や新規外部API導入は含まない。

## 2. 現行実装の要点

### 2.1 DBモデル

references は `external_references` テーブルに保存される。

- `symbol_id`
- `alert_event_id`
- `reference_type`
- `title`
- `source_name`
- `source_url`
- `published_at`
- `summary_text`
- `metadata_json`
- `dedupe_key`
- `relevance_score`

`reference_type` は現行実装では文字列で保持し、実質的に次の3種を使う。

- `news`
- `disclosure`
- `earnings`

### 2.2 取得経路

`backend/src/references/collector.ts` の現行実装では、source は次の3経路で構成される。

- `news`
  - Google News RSS search
  - `source_name = google_news_rss`
- `disclosure`
  - TDnet 日次開示一覧
  - `source_name = tdnet_disclosure`
- `earnings`
  - TDnet 日次開示一覧から決算系タイトルを抽出
  - `source_name = tdnet_earnings`

### 2.3 収集ジョブの入口

- TradingView webhook 後:
  - `collect_references_for_alert`
  - `generate_alert_summary`
- Symbol AI論点カード生成時:
  - 既存 `external_references` を選択して使用
- Comparison AI比較総評生成時:
  - 各 symbol の `related_references` を合算して使用

## 3. references供給状況の棚卸し

2026-05-01 時点の実DB観測結果は次のとおり。

| symbol | display_name | news | disclosure | earnings | latest_ref_at | latest_source | 備考 |
|---|---|---:|---:|---:|---|---|---|
| 7203 | トヨタ自動車 | 6 | 0 | 0 | 2026-03-30T07:00:00Z | google_news_rss | news 偏重 |
| 6758 | ソニーグループ | 0 | 0 | 0 | null | null | references なし |

補足:

- `external_references` の実データは、観測時点では `7203 / news` のみ存在した。
- `disclosure` と `earnings` は collector 実装自体は存在するが、観測時点のDBには保存されていなかった。
- `6758` は symbol 自体は存在するが、references は 0件だった。

## 4. 取得経路ごとの現状

### 4.1 news

- 取得想定:
  - Google News RSS
- 観測結果:
  - 実DBに 6件存在
- 特徴:
  - 最も供給されやすい
  - 銘柄名ベース検索のため、一般ニュース寄りになることがある

### 4.2 disclosure

- 取得想定:
  - TDnet 日次開示一覧
- 実装:
  - symbol code / tradingview symbol / display name を使って TDnet 行を照合
- 観測結果:
  - 実DBでは 0件
- 0件の解釈:
  - collector 未実装ではない
  - 観測期間に該当開示がなかった、または照合条件に一致しなかった可能性がある

### 4.3 earnings

- 取得想定:
  - TDnet 日次開示一覧から決算関連タイトルを抽出
- 実装:
  - `決算短信` `四半期決算` `業績予想` `業績修正` などのキーワード判定
- 観測結果:
  - 実DBでは 0件
- 0件の解釈:
  - collector 未実装ではない
  - 観測期間の対象銘柄に決算系開示がなかった、または title 判定に入らなかった可能性がある

## 5. 0件時の画面/API/AI挙動

### 5.1 SymbolDetail

- API:
  - `GET /api/symbols/:symbolId` は `related_references: []` を返す
- 画面:
  - 0件時は空状態メッセージを表示
  - 2026-05-01 の最小修正で、`news / disclosure / earnings` 内訳と、AI論点カード側の参照不足メモを表示するようにした
- AI:
  - `POST /api/symbols/:symbolId/ai-summary/generate` は references 0件でも実行可能
  - `generation_context_json.reference_count` には実際の件数が残る

### 5.2 Comparison

- API:
  - `GET /api/comparisons/:comparisonId` は各 symbol の `related_references` を返す
- 画面:
  - 0件時は symbol card 側で空状態を表示
  - 2026-05-01 の最小修正で、比較全体の参照内訳、各 symbol の内訳、AI比較総評側の参照不足メモを表示するようにした
- AI:
  - `POST /api/comparisons/:comparisonId/generate` は references 0件でも実行可能
  - `generation_context_json.reference_count` に比較時点の合算件数が残る

### 5.3 Alert summary

- API:
  - `GET /api/alerts/:alertId/summary`
  - `latest_job` で最新の `generate_alert_summary` 状態を確認できる
- 収集:
  - webhook 後に `collect_references_for_alert` が走る
  - `response_payload.saved_count / skipped_count / ref_ids` が `ai_jobs` に残る
- AI:
  - references 不足時でも summary 生成自体は試行される
  - provider failure 時は `latest_job.error_message` で追跡する

### 5.4 Home AIデイリーサマリー

- Home 画面は `daily_summary.insufficient_context` を表示できる
- ただし、references 数から機械的に `insufficient_context=true` を立てているわけではない
- 実運用では `body_markdown` の内容と references の実数を併せて確認する必要がある

## 6. 実観測で分かった注意点

### 6.1 `insufficient_context` は reference_count 0 と常に一致しない

実DBでは、`generation_context_json.reference_count = 0` の symbol thesis summary が複数存在したが、`structured_json.insufficient_context` は `false` のものがあった。

これは現時点では次の意味を持つ。

- `insufficient_context` は AI 出力依存であり、reference count から自動補正されていない
- 「references が足りないか」を判定するには、次を併せて確認する必要がある
  - 画面上の reference 内訳
  - `generation_context_json.reference_count`
  - `structured_json.insufficient_context`

### 6.2 Comparison は片側だけ references が少なくても生成できる

- 比較総評は、比較対象ごとの均等性を保証していない
- 片側だけ references 0件でも、もう片側の references と snapshot に寄って summary が出る可能性がある

### 6.3 alert summary の失敗は references 不足とは限らない

観測時点では最新 `generate_alert_summary` job は failed だったが、原因は `LocalLLM connection failed` であり、references 収集失敗そのものではなかった。

## 7. references取得失敗時の確認手順

### 7.1 画面

1. Home の `最新アラート`
2. SymbolDetail の `最近のアラート`
3. SymbolDetail / ComparisonDetail の `関連参照`

### 7.2 API

- `GET /api/alerts/:alertId`
  - `related_references`
- `GET /api/alerts/:alertId/summary`
  - `latest_job.status`
  - `latest_job.error_message`
- `GET /api/symbols/:symbolId`
  - `related_references`
- `GET /api/comparisons/:comparisonId`
  - `symbols[].related_references`

### 7.3 DB直接確認（secret非参照）

銘柄別件数:

```sql
select
  s.symbol_code,
  s.display_name,
  er.reference_type,
  count(*) as ref_count,
  max(er.published_at) as latest_published_at
from external_references er
left join symbols s on s.id = er.symbol_id
group by s.symbol_code, s.display_name, er.reference_type
order by s.symbol_code, er.reference_type;
```

直近ジョブ:

```sql
select
  job_type,
  status,
  created_at,
  completed_at,
  error_message
from ai_jobs
where job_type in (
  'collect_references_for_alert',
  'generate_alert_summary',
  'generate_symbol_thesis_summary',
  'generate_comparison_summary'
)
order by created_at desc
limit 20;
```

## 8. 実運用での確認ポイント

1. まず `external_references` の件数を銘柄別・type別に見る
2. `news だけある状態` と `0件状態` を分けて扱う
3. AI summary の本文だけで「参照十分」と判断しない
4. `insufficient_context` が `false` でも、reference_count 0 なら参照不足として扱う
5. alert summary failed は `collect_references_for_alert` と `generate_alert_summary` を分けて確認する

## 9. 今回の最小修正

2026-05-01 時点で次の軽微修正を入れた。

- SymbolDetail
  - references 内訳表示
  - references 0件時の AI論点カード補助文言
- ComparisonDetail
  - 比較全体の references 内訳表示
  - symbol card ごとの references 内訳表示
  - references 0件時の AI比較総評補助文言

## 10. 次タスク候補

### 優先度: 高

- `reference_count = 0` の AI summary で `insufficient_context` をどう一貫付与するか整理する

### 優先度: 中

- disclosure / earnings が 0件の理由を、collector 実行ログと TDnet 照合条件単位で切り分ける
- references 収集結果の type 別件数を API または admin 向け view で見やすくする

### 優先度: 低

- references 一括再収集の内部運用手順を追加する
- SymbolDetail / Comparison で最新 published_at を見やすく出す

## 11. 2026-05-01 追加整理: insufficient_context 正規化

- 対象 summary
  - Symbol thesis summary
  - Comparison summary
  - Alert summary
  - Home daily summary
- 補正式
  - normalized_insufficient_context = provider_insufficient_context || reference_count === 0
- 保存時
  - 新規生成 summary は structured_json.insufficient_context を補正して保存する
  - generation_context_json.reference_count は従来どおり保存する
- 表示時
  - 既存履歴はDB一括更新しない
  - API/view 返却時に generation_context_json.reference_count を見て補正表示する
- 補足
  - references 0件は「情報が一切ない」ではなく「参照情報が不足」として扱う
  - provider が insufficient_context = true を返した場合も false に戻さない
  - disclosure / earnings 0件の根因調査は今回対象外

## 12. 2026-05-02 追加整理: TDnet disclosure / earnings が 0件のときの切り分け

- 0件 reason 一覧
  - `tdnet_fetch_failed`
  - `tdnet_no_file_for_date`
  - `tdnet_parse_zero_rows`
  - `tdnet_rows_exist_but_no_symbol_match`
  - `tdnet_symbol_match_but_no_earnings_title`
  - `tdnet_no_matching_disclosure_in_lookback`
  - `tdnet_no_matching_earnings_in_lookback`
- collector 診断で残す情報
  - `dates_checked`
  - `fetched_dates`
  - `fetch_failed_dates`
  - `no_file_dates`
  - `parse_zero_row_dates`
  - `rows_parsed`
  - `symbol_matches`
  - `earnings_candidates`
  - `returned_count`
  - `reason`
  - `checked_dates`
  - `per_date[]`
- 運用上の見方
  - `tdnet_fetch_failed`: 403/timeout/一時障害などで一覧取得自体が失敗している
  - `tdnet_no_file_for_date`: 対象日の一覧ファイルが存在しない。土日祝や対象日未掲載の可能性が高い
  - `tdnet_parse_zero_rows`: HTML は取得できたが、既存 parser が row を 0 件しか取れていない。TDnet HTML 構造変更の疑いがある
  - `tdnet_rows_exist_but_no_symbol_match`: 一覧 row は取れているが、symbol code / display name / tradingview symbol の照合で落ちている
  - `tdnet_symbol_match_but_no_earnings_title`: symbol 一致 row はあるが、現行 earnings keyword に入っていない
  - `tdnet_no_matching_disclosure_in_lookback`: disclosure 対象期間内に該当開示がなかった
  - `tdnet_no_matching_earnings_in_lookback`: earnings 対象期間内に決算系タイトルがなかった
- 2026-05-02 の 1 回確認結果
  - 対象 URL: `I_list_001_20260501.html`
  - HTTP status: `200`
  - parsed row count: `100`
  - `7203` / `6758` 該当 row: `0`
  - 解釈:
    - 少なくともこの 1 日については `tdnet_parse_zero_rows` ではない
    - `7203` / `6758` の `disclosure` / `earnings` 0件は、「その日の TDnet 一覧に対象銘柄がいない」可能性が高い
    - lookback 日数、照合条件、earnings keyword のどこで 0 件になったかは diagnostics で継続確認する

## 13. 2026-05-05 追加観測: TDnet実データ 7日分の限定確認

- 観測対象
  - `7203 / トヨタ自動車 / TSE:7203`
  - `6758 / ソニーグループ / TSE:6758`
- 観測方法
  - collector 本体は変更せず、`backend/src/references/collector.ts` の `_internal.parseTdnetRows` / `_internal.matchesSymbol` / `_internal.isEarningsTitle` を使う一時観測スクリプトで確認した。
  - 対象 URL は `https://www.release.tdnet.info/inbs/I_list_001_{date}.html` の 1 日 1 取得に限定した。
  - 外部アクセスは 2026-04-27 から 2026-05-05 までの 7 日分のみ行った。
- 観測対象日
  - `2026-05-05`
  - `2026-05-04`
  - `2026-05-01`
  - `2026-04-30`
  - `2026-04-29`
  - `2026-04-28`
  - `2026-04-27`

| date | http status | rows_parsed | earnings_candidates_total | 7203 disclosure reason | 7203 earnings reason | 6758 disclosure reason | 6758 earnings reason | memo |
|---|---:|---:|---:|---|---|---|---|---|
| 2026-05-05 | 200 | 0 | 0 | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | 祝日帯の空ページとみられる。対象銘柄一致なし |
| 2026-05-04 | 200 | 0 | 0 | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | 祝日帯の空ページとみられる。対象銘柄一致なし |
| 2026-05-01 | 200 | 100 | 16 | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | row は十分あるが 7203/6758 該当なし |
| 2026-04-30 | 200 | 100 | 22 | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | row は十分あるが 7203/6758 該当なし |
| 2026-04-29 | 200 | 0 | 0 | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | tdnet_parse_zero_rows | 祝日帯の空ページとみられる。対象銘柄一致なし |
| 2026-04-28 | 200 | 100 | 34 | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | row は十分あるが 7203/6758 該当なし |
| 2026-04-27 | 200 | 100 | 25 | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | tdnet_rows_exist_but_no_symbol_match | row は十分あるが 7203/6758 該当なし |

- 集計メモ
  - `rows_parsed > 0` の日: 4 日
  - `rows_parsed = 0` の日: 3 日
  - `7203 symbol_matches > 0`: 0 日
  - `6758 symbol_matches > 0`: 0 日
  - `symbol_matches > 0 かつ earnings 0` の日: 0 日
  - 対象銘柄に一致した row title: 0 件
- 支配的な 0 件 reason
  - 実営業日相当で支配的だったのは `tdnet_rows_exist_but_no_symbol_match`
  - 祝日帯では `HTTP 200` でも `rows_parsed = 0` の空ページが返り、日単位では `tdnet_parse_zero_rows` になった
- 判定
  - parser 崩れが主因ではない
    - `2026-04-27` `2026-04-28` `2026-04-30` `2026-05-01` はいずれも `rows_parsed = 100`
  - 今回の主因は symbol 照合ロジック不一致よりも、対象日範囲に 7203 / 6758 の TDnet 開示自体が出ていない可能性が高い
    - row は十分あるが、対象銘柄一致が 0 件で連続した
  - earnings keyword 不足は今回の観測では未到達
    - `symbol_matches = 0` のため、`tdnet_symbol_match_but_no_earnings_title` を観測できていない
- 次判断
  - 次の改善優先度は `parser` ではなく、まず `lookback期間と対象銘柄の実開示有無の確認`
  - その上で、実際に symbol match が出る銘柄で `earnings keyword` 判定を追加観測する
  - 7203 / 6758 を使い続ける場合は、7 日より長い lookback を限定的に追加観測するか、開示が出た日を手動特定して再確認する

## 14. 2026-05-05 追加観測: symbol match が成立する別銘柄での earnings keyword 妥当性確認

- 観測対象日
  - `2026-05-01`
  - `2026-04-30`
  - `2026-04-28`
  - `2026-04-27`
- 観測方法
  - PR #184 で `rows_parsed > 0` を確認済みの 4 日だけを対象にした。
  - `backend/src/references/collector.ts` の `_internal.parseTdnetRows` / `_internal.normalizeTdnetCode` / `_internal.matchesSymbol` / `_internal.isEarningsTitle` / `_internal.classifyEarningsCategory` を使って実 title を確認した。
  - 外部アクセスは 4 日分の一覧取得に限定し、そこから symbol match が成立する銘柄を抽出した。
- 選定した銘柄
  - `2148 / ＩＴＭ / TSE:2148`
  - `9929 / 平和紙業 / TSE:9929`
  - `5132 / Ｇ－ｐｌｕｓｚｅｒｏ / TSE:5132`
- symbol match が成立した根拠
  - 3 銘柄とも TDnet row の `codeText` から `normalizeTdnetCode()` で 4 桁 symbol を得られた。
  - `matchesSymbol()` に `symbolCode` と `tradingviewSymbol` を渡すと、いずれも `matchedBy = code` になった。

| date | symbol | company | title | matched_by | is_earnings_title | category | memo |
|---|---|---|---|---|---|---|---|
| 2026-05-01 | 2148 | ＩＴＭ | 2026年３月期 決算短信〔ＩＦＲＳ〕（連結） | code | true | earnings_results | 決算短信を正しく earnings 扱い |
| 2026-05-01 | 9929 | 平和紙業 | 2026年３月期連結業績予想の修正に関するお知らせ | code | true | earnings_forecast | 業績予想の修正を正しく earnings 扱い |
| 2026-05-01 | 5132 | Ｇ－ｐｌｕｓｚｅｒｏ | 自己株式の取得状況に関するお知らせ | code | false | - | symbol match は成立するが earnings ではない。`tdnet_symbol_match_but_no_earnings_title` 相当 |

- 判定
  - 現時点では earnings keyword は概ね妥当と判断する。
  - 根拠
    - `決算短信` を含む title が `true`
    - `業績予想の修正` を含む title が `true`
    - `自己株式の取得状況` が `false`
  - 今回の限定観測では、明確に「決算系なのに false になる」title は見つからなかった。
- 次判断
  - 当面は keyword の即時改修は不要。
  - ただし `決算説明資料` や `月次業績` のような境界 title は、AI に渡したい範囲の仕様次第で今後見直し余地がある。
  - 次に見直すなら、symbol match が成立する銘柄で `tdnet_symbol_match_but_no_earnings_title` が多発する title 群を数件集めて、keyword 追加候補を実タイトルベースで整理する。

## 15. 2026-05-08 追加確認: disclosure / earnings の保存導線固定

- 目的
  - `symbol match` が成立するタイトルを使って、collector 出力だけでなく `external_references` 保存導線まで成立するかを固定する。
  - 外部ネットワークには依存せず、最小 fixture と fetch mock で確認する。
- 確認方法
  - `backend/test/reference-collector-save.e2e.test.ts`
    - TDnet 最小 HTML fixture を返す fetch mock を使い、`collectForSymbol()` の戻り値と diagnostics を確認
  - `backend/test/queue-handlers-tdnet.e2e.test.ts`
    - collector の戻り値を使って queue handler の保存処理を実行し、`external_references` と `ai_jobs.response_payload` を確認
- fixture で固定したタイトル
  - `2148 / ＩＴＭ`
    - `2026年３月期 決算短信〔ＩＦＲＳ〕（連結）`
    - 期待結果:
      - `disclosure = 1`
      - `earnings = 1`
  - `5132 / Ｇ－ｐｌｕｓｚｅｒｏ`
    - `自己株式の取得状況に関するお知らせ`
    - 期待結果:
      - `disclosure = 1`
      - `earnings = 0`
      - earnings diagnostics reason は `tdnet_symbol_match_but_no_earnings_title`

### 15.1 collector 出力の確認結果

- `2148`
  - `symbol_matches > 0`
  - `disclosure.returned_count = 1`
  - `earnings.returned_count = 1`
  - earnings category は `earnings_results`
- `5132`
  - `symbol_matches > 0`
  - `disclosure.returned_count = 1`
  - `earnings.returned_count = 0`
  - earnings diagnostics reason は `tdnet_symbol_match_but_no_earnings_title`

### 15.2 保存導線の確認結果

- queue handler 経由で、次が `external_references` に保存されることを確認した。
  - `reference_type = disclosure`
  - `reference_type = earnings`
  - `source_name = tdnet_disclosure`
  - `source_name = tdnet_earnings`
- `metadata_json` には次が残る。
  - `disclosure_code`
  - `query_date`
  - `match_reason`
  - queue save 時に付与される `source_type`
- `category` は collector の top-level field を queue save 時に `metadata_json.category` に転記する。
- collect job の `ai_jobs.response_payload` には次が残る。
  - `saved_count`
  - `skipped_count`
  - `ref_ids`
  - `source_breakdown`
  - `diagnostics`

### 15.3 この確認で分かったこと

- 現行実装は、`symbol match` と earnings keyword が成立するタイトルであれば、`disclosure / earnings` の両方を保存できる。
- `symbol match` は成立するが決算系タイトルでない場合、`disclosure` だけ保存し、earnings 側は `tdnet_symbol_match_but_no_earnings_title` として説明できる。
- したがって、現時点の主課題は「collector 未実装」ではなく、次のどこで 0 件になっているかを銘柄・日付単位で切り分けることにある。
  - 対象日に該当開示がない
  - symbol match が成立しない
  - earnings keyword に入らない

### 15.4 まだ残る課題

- この確認は fetch mock fixture ベースであり、TDnet 実サイトの追加観測ではない。
- `2148` や `9929` のような実開示あり銘柄について、symbol 詳細画面や alert summary の live データで `disclosure / earnings` が見えるかは別確認が必要。
- `disclosure` / `earnings` の供給改善として次に見るべき箇所は、collector 本体の全面改修ではなく、lookback 日数と対象銘柄の実開示有無の追加観測である。

## 16. 2026-05-08 追加確認: disclosure / earnings live データ成立範囲

- 目的
  - fetch mock fixture ではなく、TDnet live データ 1 日分で `disclosure / earnings` の保存・表示・alert context 反映まで確認する。
- 外部アクセス範囲
  - 対象日: `2026-05-01`
  - live fetch 回数: 1 日分のみ
  - 確認銘柄:
    - `2148 / ＩＴＭ / TSE:2148`
    - `9929 / 平和紙業 / TSE:9929`
    - `5132 / Ｇ－ｐｌｕｓｚｅｒｏ / TSE:5132`
- raw HTML / raw external response 全文は保存していない。

### 16.1 live fetch / parse 結果

- 対象 URL:
  - `I_list_001_20260501.html`
- 結果:
  - `HTTP 200`
  - `rows_parsed = 100`

| symbol | matched rows | title 概要 | earnings 判定 | memo |
|---|---:|---|---|---|
| 2148 | 2 | 決算短信 / 決算補足説明資料 | 2件とも true | `matched_by = code` |
| 9929 | 1 | 連結業績予想の修正 | true | `matched_by = code` |
| 5132 | 1 | 自己株式の取得状況 | false | `matched_by = code` |

判断:

- `2148` と `9929` は live データ上でも earnings 系 title が存在する。
- `5132` は live データ上でも disclosure のみで、earnings ではない。
- docs/33 の既存観測と同じ方向で説明できる。

### 16.2 保存導線の live 確認

live fetch した 2026-05-01 の TDnet 一覧をその場で fetch mock に流し込み、queue handler 経由の保存導線を実 DB に対して確認した。

#### 2148

- `disclosure = 2`
- `earnings = 2`
- `source_breakdown`
  - `disclosure: 2`
  - `earnings: 2`
- `diagnostics.disclosure.reason = null`
- `diagnostics.earnings.reason = null`
- `external_references` 保存確認:
  - `reference_type = disclosure / earnings`
  - `source_name = tdnet_disclosure / tdnet_earnings`
  - `metadata_json.disclosure_code = 2148`
  - `metadata_json.query_date = 20260501`
  - `metadata_json.match_reason = code`
  - `metadata_json.category` あり

#### 5132

- `disclosure = 1`
- `earnings = 0`
- `source_breakdown`
  - `disclosure: 1`
- `diagnostics.disclosure.reason = null`
- `diagnostics.earnings.reason = tdnet_symbol_match_but_no_earnings_title`
- `external_references` 保存確認:
  - `reference_type = disclosure`
  - `source_name = tdnet_disclosure`
  - `metadata_json.disclosure_code = 5132`
  - `metadata_json.query_date = 20260501`
  - `metadata_json.match_reason = code`

### 16.3 dedupe の live 確認

`2148` で同一 symbol / 同一 TDnet 一覧に対して collect を 2 回実行した結果:

- 1 回目
  - `saved_count = 4`
  - `skipped_count = 0`
- 2 回目
  - `saved_count = 0`
  - `skipped_count = 4`

判断:

- live データ由来でも dedupe は機能している。
- `ref_ids` は 2 回目も既存 reference を指して返せる。

### 16.4 SymbolDetail / alert context 反映

#### SymbolDetail API

queue handler 実行後に `GET /api/symbols/:symbolId` 相当を確認した結果:

- `2148`
  - `related_references` 内訳:
    - `disclosure: 2`
    - `earnings: 2`
- `5132`
  - `related_references` 内訳:
    - `disclosure: 1`

判断:

- disclosure / earnings は live 確認でも SymbolDetail の references 内訳に反映される。
- references 0 件時との差分は `related_references` の type 内訳で明確に見える。

#### Alert summary context

`buildAlertSummaryContext(alertEventId)` を確認した結果:

- `2148`
  - `referenceIds` に 4 件入る
  - `references[].referenceType` に `disclosure / earnings` の両方が入る
- `5132`
  - `referenceIds` に 1 件入る
  - `references[].referenceType` は `disclosure` のみ

判断:

- `collect_references_for_alert` で saved された disclosure / earnings は、alert summary context にも反映される。
- 今回は AI summary 本文生成までは進めていないが、context 反映までは確認できた。

### 16.5 今回の結論

- `disclosure / earnings` の collector は、実開示あり銘柄の live データでも成立する範囲が確認できた。
- 2026-05-01 の 1 日分に限れば:
  - `2148`: disclosure / earnings の両方が保存・表示・alert context 反映まで成立
  - `5132`: disclosure は成立、earnings は `tdnet_symbol_match_but_no_earnings_title` で説明可能
  - `9929`: live parse 上の symbol match と earnings 判定 true までは確認
- 次に足すなら、`9929` の保存導線 live 確認か、references あり銘柄での TradingView 実送信確認を優先する。

## 17. 2026-05-08 追加確認: 9929 の保存導線 live 確認

- 目的
  - `9929 / 平和紙業 / TSE:9929` で、`disclosure / earnings` の collector 出力と queue handler 経由の保存導線が live データでも成立するかを確認する。
- 対象日
  - `2026-05-01`
- 外部アクセス範囲
  - TDnet `I_list_001_20260501.html` のみ
  - raw HTML 全文は保存していない
- 確認方法
  - `triggered_at = 2026-05-01T06:00:00Z` 相当の 1 日限定 collector config で `collectForAlert()` を実行
  - 返却された refs を queue handler に流し、`external_references` 保存と `ai_jobs.response_payload` を確認
  - その後 `GET /api/symbols/:symbolId` 相当で `related_references` 内訳を確認

### 17.1 collector 出力

- `disclosure = 1`
- `earnings = 1`
- title
  - `2026年３月期連結業績予想の修正に関するお知らせ`
- `matched_by = code`
- `earnings.category = earnings_forecast`
- metadata
  - `disclosure_code = 9929`
  - `query_date = 20260501`
  - `match_reason = code`

### 17.2 diagnostics

- `disclosure.reason = null`
- `earnings.reason = null`
- `disclosure`
  - `dates_checked = 1`
  - `rows_parsed = 100`
  - `symbol_matches = 1`
  - `returned_count = 1`
- `earnings`
  - `dates_checked = 1`
  - `rows_parsed = 100`
  - `symbol_matches = 1`
  - `earnings_candidates = 16`
  - `returned_count = 1`

### 17.3 保存導線

- `external_references`
  - `reference_type = disclosure`
  - `reference_type = earnings`
  - `source_name = tdnet_disclosure`
  - `source_name = tdnet_earnings`
- `metadata_json`
  - `disclosure_code = 9929`
  - `query_date = 20260501`
  - `match_reason = code`
  - `category = financial_results / earnings_forecast`
- `ai_jobs.response_payload`
  - `saved_count = 2`
  - `skipped_count = 0`
  - `source_breakdown = { disclosure: 1, earnings: 1 }`
  - diagnostics に `reason = null` を保持

### 17.4 SymbolDetail API 相当の確認

- `GET /api/symbols/:symbolId` 相当の `related_references` 内訳
  - `disclosure: 1`
  - `earnings: 1`

### 17.5 判断

- `9929` でも `disclosure / earnings` の保存導線は成立した
- これにより、`2148` / `5132` / `9929` で次が切り分け済みになった
  - `2148`: disclosure / earnings 両方成立
  - `5132`: disclosure のみ、earnings は `tdnet_symbol_match_but_no_earnings_title`
  - `9929`: disclosure / earnings 両方成立、earnings category は `earnings_forecast`
- `disclosure / earnings` direct type の TradingView 実送信再確認は、現時点では実施しない
  - `2148` 実送信で webhook 導線自体は確認済み
  - `2148` の TradingView payload互換疑似送信で direct type が alert summary context / 本文へ入ることも確認済み
  - 現行 env では alert 用 lookback が `disclosure=3日`, `earnings=7日` のため、開示日から離れた実送信で direct type を拾う再現性が低い
  - 実送信では過去の `triggered_at` を任意固定できないため、現時点の追加実送信の検証価値は低い
  - 必要なら実開示日近傍の任意後続確認とする
