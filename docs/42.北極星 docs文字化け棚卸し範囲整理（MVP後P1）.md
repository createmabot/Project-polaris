# 北極星 docs文字化け棚卸し範囲整理（MVP後P1）

更新日: 2026-05-08

## 1. 目的
- docs 全体の文字化け棚卸し対象を決める。
- 修復優先度を分類する。
- 旧 docs の扱いと、推測修復しない方針を明確にする。

## 2. 前提
- MVP は完了済みであり、現行運用の runbook を優先する。
- 全 docs を同一優先度で一括修復しない。
- 本文推測による復元は行わない。
- secret / token / shared_secret / API key の実値は扱わない。
- 文字化け棚卸しでは、本文が読めるか、現行運用に使うか、正本か、を分けて判断する。
- ローカル PowerShell 表示上の文字化けは、ファイル実体の文字化けと区別する。
- ファイル実体の可読性判定は、`Get-Content -Encoding UTF8 -Raw`、`git show`、GitHub 表示のいずれかで再確認してから行う。

## 3. 分類基準
### A. 現行runbook / 最優先修復対象
条件:
- 現在の開発・運用で頻繁に使う。
- MVP 後も継続参照する。
- 新しい作業者が最初に読む。

対応方針:
- 文字化けがあれば優先修復する。
- secret 非露出も同時確認する。
- P1 または P2 先頭の修復対象に置く。

### B. 現行仕様の正本 / 高優先修復対象
条件:
- 現行実装判断に使う。
- API / 画面 / データモデル / Rule Lab の正本として参照される。
- 文字化けしていると今後の実装判断に影響する。

対応方針:
- 現行実装に効く章から優先修復する。
- 新 docs に正本が移った箇所は、旧本文全面修復より参照先明記を優先してよい。

### C. 履歴資料 / 低優先または修復対象外候補
条件:
- 旧スプリント資料。
- すでに新しい docs に内容が吸収済み。
- 現行運用で直接参照しない。
- 文字化けしていても実務影響が小さい。

対応方針:
- 原則として一括修復しない。
- 必要なら「履歴資料」「参照注意」として扱う。
- 本文推測修復は避ける。

### D. 判断保留
条件:
- 文字化けがあるが現行影響が判断しにくい。
- 一部だけ現行仕様として使われている。
- 参照頻度が不明。

対応方針:
- 判断理由を記録する。
- 次タスクで個別確認する。

## 4. 棚卸し結果サマリ

| 分類 | 対象docs | 文字化け状況 | 現行影響 | 対応方針 |
|---|---|---|---|---|
| A | `README.md`, `docs/walkthrough.md`, `docs/0.目次.md`, `docs/32`〜`docs/41` | UTF-8 明示読み取りと GitHub 表示基準では、現時点で可読。PowerShell 既定表示だけで文字化け判定しない。 | 高 | 直ちに本文修復対象とはせず、現行 runbook として維持する。文字化け疑いがあれば UTF-8 明示読み取りで再確認する。 |
| B | `docs/1`, `docs/2`, `docs/3`, `docs/8`, `docs/17`, `docs/25`, `docs/29`, `docs/30` | `docs/25` と `docs/29` は UTF-8 明示読み取りと GitHub 表示で可読。`docs/30` は前半可読だが、後半に UTF-8 明示読み取りでも文字化けが残る。MVP初期の API sample docs は legacy numbered docs cleanup PR 2 で削除済み。 | 中〜高 | 実装判断に使う章を優先して再確認する。`docs/30` は全文復元せず、後続 docs と実装を正本参照先として補強する。 |
| C | `docs/15`, `docs/18`〜`docs/28`, 補足レポート類 | 一部に文字化け可能性があるが、現行 runbook としては使わない。 | 低 | 原則後回し。必要なら履歴資料注記のみ。 |
| D | `docs/31`, `docs/24`, `docs/14`, `docs/7`, `docs/9`〜`docs/13` | 現行参照頻度と可読性の影響が混在。PowerShell 表示だけでは判断しない。 | 中 | UTF-8 明示読み取りと GitHub 表示で再確認し、A/B/C へ再配置する。 |

## 5. A. 現行runbook / 最優先修復対象
対象:
- `README.md`
- `docs/walkthrough.md`
- `docs/0.目次.md`
- `docs/32.北極星 TradingView実送信 webhook運用手順（MVP）.md`
- `docs/33.北極星 references供給状況整理と運用課題（MVP）.md`
- `docs/34.北極星 TradingView CSV import 運用手順（MVP）.md`
- `docs/35.北極星 実データAI品質確認メモ（MVP）.md`
- `docs/36.北極星 MVP受入確認チェックリスト（MVP）.md`
- `docs/37.北極星 MVP受入確認結果（MVP）.md`
- `docs/38.北極星 MVP完了報告（MVP）.md`
- `docs/39.北極星 MVP後ロードマップ・バックログ整理.md`
- `docs/40.北極星 Home card 表示件数・並び順・snapshot依存仕様（MVP後P1）.md`
- `docs/41.北極星 Rule Lab日本語入力 UI経路再確認メモ（MVP後P1）.md`

確認結果:
- `README.md`, `docs/walkthrough.md`, `docs/0.目次.md`, `docs/32`〜`docs/41` は、UTF-8 明示読み取りと GitHub 表示基準では現時点で運用に使える。
- `docs/32`, `docs/33` は PowerShell の既定表示では文字化けして見える場合があるが、ファイル実体としては可読だった。

判断:
- A 分類は現行 runbook 群として維持する。
- `docs/32`, `docs/33` は直ちに本文修復対象とはしない。
- 文字化け疑いが再発した場合も、PowerShell 表示だけでなく UTF-8 明示読み取りと GitHub 表示で再判定する。

## 6. B. 現行仕様の正本 / 高優先修復対象
対象:
- `docs/1.株価評価AIツール：北極星.md`
- `docs/2.北極星 データモデル設計（MVP）.md`
- `docs/3.北極星 API ユースケース単位の入出力設計（MVP）.md`
- `docs/8.北極星 自然言語→Pine変換フロー設計（MVP）.md`
- MVP初期の API sample docs（legacy numbered docs cleanup PR 2 で削除済み。現行は `docs/仕様書/03_API仕様.md` と tests を優先）
- `docs/17.北極星 画面別の入力 出力サンプル（MVP）.md`
- `docs/25. 補助資料_1 北極星 ホームデータ供給仕様（MVP）.md`
- `docs/29. 北極星 ルール検証ラボMVP 画面 API 保存設計 再整理（実装前固定）.md`
- `docs/30. 北極星 ルール検証ラボMVP 実装反映差分（実装後固定）.md`

確認結果:
- `docs/25` は Home 仕様の参照先として残り、UTF-8 明示読み取りと GitHub 表示では可読だった。
- Home card の詳細仕様は `docs/40` に移行済みであり、`docs/25` は補助資料として扱う。
- `docs/29` は UTF-8 明示読み取りと GitHub 表示で可読であり、Rule Lab MVP の実装前固定資料として参照継続可能。
- `docs/30` は前半の画面構成 / API / 保存仕様までは可読だが、後半に UTF-8 明示読み取りでも文字化けが残る。
- MVP初期の API sample docs は削除済み。画面別 sample docs は参照継続可能。

判断:
- `docs/29` は修復不要。実装前固定の設計資料として維持する。
- `docs/30` は全文復元を行わず、「文字化けあり / 参照注意」の扱いとし、現行仕様確認は `README.md`、`docs/walkthrough.md`、`docs/39`〜`docs/44`、関連実装・テストを優先参照する。
- `docs/25` は可読な補助資料として維持し、Home card 詳細は `docs/40` を正本とする。
- `docs/17`, `docs/3`, `docs/8` は現時点で利用可能なため、修復より維持と差分確認を優先する。API sample は現行 API 仕様と tests を優先する。

## 7. C. 履歴資料 / 低優先または修復対象外候補
対象候補:
- `docs/15`〜`docs/28` の旧計画・補助資料群
- `docs/webhook.md`
- `docs/webhook-refactor-report.md`
- `docs/webhook-final-compliance-report.md`
- `docs/snapshot-weekly-reviews/*`

確認結果:
- 現行運用の正本は `docs/32`〜`docs/41` に移っており、これらは履歴・補助資料の性格が強い。
- 本文を推測修復しても、現行仕様判断への寄与が薄い。

判断:
- 原則後回し。
- 必要になった docs だけ個別修復、または「履歴資料 / 参照注意」注記で十分。

## 8. D. 判断保留
対象候補:
- `docs/31.北極星 APIレスポンス差分棚卸しと次タスク候補.md`
- `docs/24.北極星 開発着手用 README セットアップ手順書（MVP）.md`
- `docs/14.北極星 TradingView 設定手順書（MVP）.md`
- `docs/7.北極星 TradingView alert message テンプレート設計（MVP）.md`
- `docs/9`〜`docs/13`

理由:
- 一部は今後も参照しうるが、実際の参照頻度が runbook 群ほど高くない。
- 文字化けの有無だけでなく、内容が新 docs に吸収済みかの判断が必要。

次の扱い:
- UTF-8 明示読み取りと GitHub 表示で再確認したうえで、A/B/C のどこへ寄せるかを判断する。

## 9. 修復方針
- 文字化けが軽微な場合は直接修正する。
- 本文推測が必要な場合は修復しない。
- 新しい docs が正本になっている場合は、旧 docs 修復より参照先明記を優先する。
- 主要 runbook に文字化け疑いが出た場合も、まず UTF-8 明示読み取りと GitHub 表示で実体確認する。
- 実体として可読な docs は、PowerShell 表示だけを根拠に修復対象へ入れない。
- 修復時も secret 非露出確認を必須にする。
- UTF-8 BOM の有無だけを問題化せず、実際に可読性と運用影響で判断する。

## 10. 次タスク候補
1. UTF-8 明示読み取りと GitHub 表示を基準に、A/B/D 分類 docs の再棚卸しを進める。
2. `docs/30` のように UTF-8 明示読み取りでも後半破損が残る docs は、全文修復ではなく参照注意と正本参照先の整理を優先する。
3. C 分類 docs に `履歴資料 / 参照注意` 注記を付けるか検討する。
4. docs 全体の文字コード / BOM / 保存方針を整理する。

## 11. Codex修復依頼テンプレート
以下をベースに、個別 docs の修復を依頼する。

```text
北極星プロジェクトの docs 修復タスクとして、<対象docs> の文字化け修復可否を確認してください。

前提:
- docs 配下を正本として扱う
- 推測による本文復元はしない
- secret / token / shared_secret / API key の実値は残さない
- まず現行 runbook / 正本 / 履歴資料のどれかを判断する

やってほしいこと:
1. 対象 docs の文字化け範囲を確認する
2. 現行運用・正本性・参照頻度を確認する
3. 軽微修復で済むか、推測修復が必要かを判定する
4. 修復する場合は最小差分で行う
5. 修復しない場合は、参照先 docs への誘導や注記で代替できるかを提案する

最終報告に必ず含めること:
- 対象 docs
- 文字化け範囲
- 正本性の判定
- 修復可否
- 修復しない場合の理由
- secret 非露出確認
```
