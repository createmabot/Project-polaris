# 30. 北極星 ルール検証ラボMVP 実装反映差分（実装後固定）

更新日: 2026-03-28

## 1. 本資料の位置づけ

本資料は、`docs/29. 北極星 ルール検証ラボMVP 画面 API 保存設計 再整理（実装前固定）.md` を基準として、その後に実装へ反映されたルール検証ラボMVPの差分を、**実装後固定の正本**として整理するものである。

運用上の位置づけは以下のとおりとする。

- `docs/29` は、ルール検証ラボMVPの**実装前固定資料**として保持する。
    
- 本資料は、ルール検証ラボMVPの**実装後固定資料**として扱う。
    
- 今後、ルール検証ラボ関連の**現行仕様確認**を行う際は、本資料を優先参照とする。
    
- 一方、実装着手前の意図、MVP境界、後回し項目の確認が必要な場合に限り、`docs/29` を参照する。
    

---

## 2. 本資料で反映対象とする範囲

本資料で対象とするのは、ルール検証ラボMVPにおいて、実装済みとなった以下の領域である。

1. Strategy 作成 / Strategy Version 作成 / Pine 生成
    
2. Backtest 作成 / CSV 取込 / parse 状態保存
    
3. Backtest 詳細 / Backtest 履歴一覧
    
4. Strategy Version 一覧 / Strategy Version 詳細
    
5. Strategy Version の clone
    
6. 派生 version の軽微編集 / 再生成
    
7. clone 元との差分表示
    
8. Pine 差分の軽量表示 / 抜粋表示
    
9. 一覧の検索 / ページネーション / URL クエリ同期 / return 復帰
    
10. E2E / CI / required check / runbook による運用固定化
    

一方、以下の項目は引き続き本資料の対象外、もしくは未対応領域として整理する。

- 高度な version 比較 UI
    
- フォワード検証ノートの高度機能（添付/スレッド/通知/検索）
    
- 北極星内バックテストエンジン
    
- 高度な複合検索 / フィルタ / ソート
    
- 複数 version の同時比較
    
- 高度な比較レポートの生成
    

---

## 3. 現在の画面構成

### 3-1. Strategy / Version 系画面

- `/strategy-lab`
    
- `/strategies/:strategyId/versions`
    
- `/strategy-versions/:versionId`
    

### 3-2. Backtest 系画面

- `/backtests`
    
- `/backtests/:backtestId`
    

---

## 4. 現在のユーザーフロー

### 4-1. 新規作成フロー

1. `/strategy-lab` にて自然言語ルールを入力する。
    
2. strategy を作成する。
    
3. strategy version を作成する。
    
4. Pine を生成する。
    
5. TradingView 上で一次検証を行う。
    
6. backtest を作成する。
    
7. CSV を取り込む。
    
8. backtest 詳細画面で parse 結果および summary を確認する。
    

### 4-2. version 再利用フロー

1. `/strategies/:strategyId/versions` にて version 一覧を確認する。
    
2. 対象 version を `/strategy-versions/:versionId` で開く。
    
3. 既存 version を再生成する、または clone により新 version を作成する。
    
4. 派生 version を軽微編集する。
    
5. Pine を再生成する。
    
6. clone 元との差分を確認する。
    

### 4-3. 履歴振り返りフロー

1. `/backtests` で過去の検証履歴を探す。
    
2. `/backtests/:backtestId` を開く。
    
3. parse 状態、summary、import 履歴を確認する。
    
4. 「次アクション（Rule Lab）」から、当該検証に関連する Strategy Version の見直し導線へ戻る。
    

※ Backtest Detail には「使用した Strategy」セクションを実装済みとし、実行時 snapshot と current の導線を分離して参照できる。

---

## 5. 現在の API 一覧

### 5-1. Strategy / Version 系 API

- `POST /api/strategies`
    
- `POST /api/strategies/:strategyId/versions`
    
- `GET /api/strategies/:strategyId/versions`
  - query: `q`, `page`, `limit`, `status`, `sort(created_at|updated_at)`, `order(asc|desc)`
    
- `GET /api/strategy-versions/:versionId`
    
- `PATCH /api/strategy-versions/:versionId`
    
- `POST /api/strategy-versions/:versionId/pine/generate`
    
- `POST /api/strategy-versions/:versionId/clone`
    

### 5-2. Backtest 系 API

- `POST /api/backtests`
    
- `POST /api/backtests/:backtestId/imports`
    
- `GET /api/backtests`
  - query: `q`, `page`, `limit`, `status`, `sort(created_at|updated_at)`, `order(asc|desc)`
    
- `GET /api/backtests/:backtestId`
    
  - `used_strategy` と実行時 `snapshot` を返却し、当時の strategy 情報と current 導線を分離して参照可能
    
- `GET /api/backtests/:backtestId/imports`
    
### 5-3. 内製バックテスト土台 API（最小）

- `POST /api/internal-backtests/executions`
  - execution job を `queued` で作成し、queue enqueue まで行う（最小土台）
  - execution input snapshot の最小検証（`strategy_rule_version_id` と `data_range` は必須、`market/timeframe` は optional）を実施
  - `market/timeframe` は request 値を優先し、未指定時は strategy version の値で補完する
- `GET /api/internal-backtests/executions/:executionId`
  - execution status の source of truth を返却
- `GET /api/internal-backtests/executions/:executionId/result`
  - `succeeded` 以外は `RESULT_NOT_READY` を返却（最小ガード）
  
worker 骨組み（BullMQ）により、`queued -> running -> succeeded|failed` の最小状態遷移が成立している。
`result` は `result_summary.schema_version = "1.0"` の最小スキーマで保存・返却する。
`artifact_pointer` も `type` / `execution_id` / `path` の最小スキーマを runtime 検証して保存・返却する。
worker は status 遷移と永続化に責務を絞り、実行処理本体は service / adapter 境界へ分離する。
    

---

## 6. 現在の保存仕様

### 6-1. strategy_rules

strategy 単位の基本情報を保持する。

### 6-2. strategy_rule_versions

以下の情報を version 単位で保持する。

- `naturalLanguageRule`
    
- `normalizedRuleJson`
    
- `generatedPine`
- `forwardValidationNote`
- `forwardValidationNoteUpdatedAt`
    
- `warningsJson`
    
- `assumptionsJson`
    
- `market`
    
- `timeframe`
    
- `status`
    
- `clonedFromVersionId`
    

### 6-3. backtests

以下の情報を backtest 単位で保持する。

- `strategyRuleVersionId`
    
- `strategySnapshotJson`（実行時 strategy snapshot）
    
- `title`
    
- `executionSource`
    
- `market`
    
- `timeframe`
    
- `status`

`strategySnapshotJson` は backtest 実行時点の strategy/version 情報を保持し、後から current version が更新されても履歴再現性を維持する。
    

### 6-4. backtest_imports

以下の情報を import 単位で保持する。

- `fileName`
    
- `fileSize`
    
- `contentType`
    
- `rawCsvText`
    
- `parseStatus`
    
- `parseError`
    
- `parsedSummaryJson`
    
### 6-5. internal_backtest_executions（最小土台）

以下の情報を内製 execution job 単位で保持する。

- `strategyRuleVersionId`
- `status`（`queued` / `running` / `succeeded` / `failed` / `canceled`）
- `requestedAt` / `startedAt` / `finishedAt`
- `inputSnapshotJson`
- `resultSummaryJson`（nullable）
- `artifactPointerJson`（nullable）
- `errorCode` / `errorMessage`（nullable）
- `engineVersion`

`backtests` は TradingView / CSV import 履歴を保持し、`internal_backtest_executions` は北極星内実行状態の正本を保持する。
また worker 最小実装では、成功時に `resultSummaryJson` / `artifactPointerJson` を最小保存し、失敗時は `errorCode` / `errorMessage` を保持する。
`resultSummaryJson` は schema_version 付き最小 summary（market/timeframe/period/metrics/engine/notes）で固定する。
`artifactPointerJson` は最小 shape として `type` / `execution_id` / `path` を保持する。
    

---

## 7. Strategy Version の現仕様

### 7-1. version 一覧

version 一覧では、少なくとも以下の情報を確認可能とする。

- 派生 version かどうか
    
- clone 元との差分があるかどうか
    
- 要確認差分バッジ（`派生` かつ `差分あり` の最小強調）
- フォワード検証ノート有無（`検証ノートあり` の最小表示）
- `要確認差分` かつ `検証ノートあり` の複合シグナルを `最優先確認` として最小強調表示
- `検証ノートあり` の行でノート更新目安を最小表示（`forwardValidationNoteUpdatedAt` 基準）
- `forwardValidationNoteUpdatedAt` がページ内で最も新しい行を `最新ノート` として最小表示し、先読み対象を判別しやすくする
- `最優先確認` かつ `最新ノート` の行を `今読む候補` として最小表示し、優先読了対象を即判別可能にする
    
- status
    
- market / timeframe
    
- warnings の有無
    
- 作成日時
    

### 7-2. version 詳細

version 詳細では、少なくとも以下の情報を確認可能とする。

- natural language rule
    
- generated Pine
    
- warnings
    
- assumptions
    
- status
    
- market / timeframe
    
- strategy id / version id
    
- createdAt / updatedAt
- フォワード検証ノートの更新目安（`forwardValidationNoteUpdatedAt`。ノート未設定時は `-`）
- 比較サマリの優先確認ポイント（ルール文 / Pine / status の確認順）
- 比較サマリ本体での判断順表示（naturalLanguageRule / Pine / status / updatedAt を変更有無つきで提示）
- Pine 差分抜粋の区分表示（変更 / 追加 / 削除）
- Pine 差分抜粋の確認順ガイド（変更 → 追加 → 削除）
    

### 7-3. clone の現仕様

- 元 version は更新しない。
    
- 同一 strategy 配下に新 version を作成する。
    
- clone 元参照を保持する。
    
- clone 後は新 version の detail 画面へ遷移する。
    

### 7-4. 軽微編集の現仕様

- 編集対象の主対象は `naturalLanguageRule` とする。
    
- 保存時は再生成前提の状態へ戻す。
    
- 具体的には以下の状態へ更新する。
    
    - `generatedPine = null`
        
    - `normalizedRuleJson = null`
        
    - `warnings / assumptions` はクリア
        
    - `status = draft`
        

### 7-5. 再生成の現仕様

- 同一 version を更新対象とする。
    
- `generatedPine`、`warnings`、`assumptions`、`status` を再計算する。
    

---

## 8. 差分表示の現仕様

### 8-1. 比較対象

- 比較対象は clone 元 version のみとする。
    
- `clonedFromVersionId` を正とする。
    

### 8-2. 表示項目

以下を差分表示対象とする。

- naturalLanguageRule 差分
    
- status 比較
    
- updatedAt 比較
    
- Pine の変更あり / なし
    
- Pine の行差分 / 文字差分
    
- Pine 差分抜粋（先頭 N 件）
    

### 8-3. 比較元が無い場合

- 「比較元の version はありません」と表示する。
    
- 画面は壊さず、通常操作を継続可能とする。
    

---

## 9. 一覧系の現仕様

### 9-1. Backtests 一覧

- タイトル部分一致検索
    
- API ページネーション
    
- status フィルタ
    
- sort / order（`created_at|updated_at` × `asc|desc`）
    
- URL クエリ同期（`q`, `page`, `status`, `sort`, `order`）
    
- 詳細からの return 復帰
    
- 実行時 Strategy / Version の最小表示
- Rule Lab の version 詳細へ戻る最小導線（見直し導線）
    

### 9-2. Strategy Version 一覧

- naturalLanguageRule 部分一致検索
    
- API ページネーション
    
- status フィルタ
    
- sort / order（`created_at|updated_at` × `asc|desc`）
    
- URL クエリ同期（`q`, `page`, `status`, `sort`, `order`）
    
- 詳細からの return 復帰
    
- 派生 / 差分あり / status の最小見える化
    
- 要確認差分の件数表示（ページ内）と最小強調表示
- フォワード検証ノートの有無を最小表示（比較後の次検証方針の有無を一覧段階で判別）
- `要確認差分` / `検証ノートあり` / `要確認差分かつ検証ノートあり` のページ内件数表示
- `最新ノート`（ページ内で最も新しいノート更新）の件数表示と行バッジ表示
- `最優先確認` かつ `最新ノート` の件数表示と行バッジ表示（`今読む候補`）
- `今読む候補` がある場合、件数サマリからページ内先頭の対象へジャンプ可能
- `今読む候補` が複数件ある場合、件数サマリから `次の今読む候補へ` で順送り確認が可能
- `要確認差分かつ検証ノートあり` がある場合、件数サマリからページ内先頭の `最優先確認` カードへジャンプ可能
- `最優先確認` が複数件ある場合、件数サマリから `次の最優先確認へ` で順送り確認が可能
- ジャンプ直後は対象カードを一時ハイライトし、見失いにくさを最小補助（自動解除）
- `StrategyVersionDetail` でも、同一一覧文脈に `最優先確認` が複数件ある場合は `次の最優先確認へ` で順送り確認が可能
    

---

## 10. URL クエリ同期 / return 復帰の現仕様

### 10-1. Backtests 側

- `/backtests?q=...&page=...&status=...&sort=...&order=...` を一覧状態の正とする。
    
- detail へは `return` 付きで遷移する。
    
- detail から一覧へ戻ると元状態を復元する。
    

### 10-2. Strategy Versions 側

- `/strategies/:strategyId/versions?q=...&page=...&status=...&sort=...&order=...` を一覧状態の正とする。
    
- detail へは `return` 付きで遷移する。
    
- detail から一覧へ戻ると元状態を復元する。
    

---

## 11. CI / required check / runbook の現仕様

### 11-1. backtests 側

- `backtests-return-flow-e2e-check`
    
- required check 化済み
    
- runbook 整備済み
    
- pending / failure / restore / success のドリル確認済み（再実施済み）

    
- drill では `BacktestsReturnFlow.e2e.test.ts`（URL復帰）と `BacktestList.test.tsx`（一覧最小表示）を確認対象とする
- 同 check で Backtest List / Detail から Rule Lab（version 詳細）へ戻る導線URLの維持も回帰固定化する
    

### 11-2. strategy versions 側

- `strategy-versions-return-flow-e2e-check`
    
- required check 化済み
    
- runbook 整備済み
    
- pending / failure / restore / success のドリル確認済み（再実施済み）

- StrategyVersionList → StrategyVersionDetail（フォワード検証ノート編集）→ List の最小 return-flow も同 check で回帰固定化済み
- `検証ノートあり` 行を起点にした List → Detail → List の return-flow も同 check で回帰固定化済み
    

### 11-3. 関連する既存 check

- `symbol-snapshot-db-integration`
    
- `snapshot-review-generator-json-check`

### 11-4. 実地ドリル証跡の正本参照先

- required-check ドリルの最新証跡（run URL / commit / PR / BLOCKED-CLEAN 遷移）は、`docs/snapshot-weekly-reviews/2026-W13-snapshot-review.md` を正本とする。
- 本資料（`docs/30`）には運用状態の要約のみを保持し、詳細証跡は週次レビュー記録へ集約する。
    

---

## 12. 現時点の到達点

現在の Rule Lab MVP は、少なくとも以下の循環が成立している。

1. 作る
    
2. 生成する
    
3. TradingView で一次検証する
    
4. 結果を取り込む
    
5. 履歴を保存する
    
6. version を見返す
    
7. clone する
    
8. 軽微編集する
    
9. 再生成する
    
10. 差分を確認する
    

したがって、Rule Lab MVP は単発のルール生成機能ではなく、**保存・再利用・改善サイクルを回せる状態** に達しているものと整理する。

---

## 12.1 フォワード検証ノート（最新反映）

- 保存単位: `Strategy Version` 単位
- 保存項目（最小）:
  - `forward_validation_note`（本文）
  - `forward_validation_note_updated_at`（ノート専用更新時刻）
  - `created_at` / `updated_at`（version 本体のタイムスタンプ）
- API:
  - `PATCH /api/strategy-versions/:versionId` で `forward_validation_note` を作成/更新/クリア
  - `GET /api/strategy-versions/:versionId` で `forward_validation_note` を取得
  - `GET /api/strategies/:strategyId/versions` / `GET /api/strategy-versions/:versionId` で `forward_validation_note_updated_at` を取得
- 画面:
  - `Strategy Version Detail` に「次の検証ノート」セクションを追加
  - 比較結果を見た後に「次の検証方針」を同じ画面で残せる
- 設計方針:
  - フォワード検証ノートは改善サイクル（一覧で探す → 詳細で比較する → 次検証を残す）に限定
  - ノート鮮度表示は `updated_at` から分離し、`forward_validation_note_updated_at` を正として扱う
  - 汎用メモ機能（添付/スレッド/通知/高度検索）には拡張しない

## 13. 未対応 / 次段に送る項目

次段候補は、以下のとおりとする。

1. version 比較 UI の次段
    
    - Pine 差分のさらなる可読化
        
    - 比較表示の強化
        
2. フォワード検証ノートの高度化（添付/履歴管理/通知）
    
3. 北極星内バックテストエンジン（後段）
   
   - 本資料時点では最小 worker 骨組みまで実装済み（本計算ロジックは未実装）
   - 次段では以下の最小単位で着手する（設計固定済み）
     - execution job（状態遷移の正本）
     - execution input snapshot（実行条件固定）
     - execution result summary（主要指標）
     - execution artifact pointer（詳細結果参照）
   - 最小 API は `start / status / result` の 3 本を基準とする
   - 状態遷移は `queued -> running -> succeeded|failed|canceled` を正とする
   - note: 「設計固定済み」であり、エンジン本体実装は未着手
   - 役割分担は維持する（TradingView: 一次検証 / 北極星: 自然言語変換・履歴保存・レポート）
    

---

## 14. `docs/29` との関係

- `docs/29` は実装前固定資料として保持する。
    
- 本資料は実装後固定資料として扱う。
    
- Rule Lab の現行仕様確認は、本資料を優先する。
    
- 実装前の意図やMVP境界確認が必要な場合に限り、`docs/29` を参照する。
